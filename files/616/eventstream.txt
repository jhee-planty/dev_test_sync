=== /ai/v2/generation EventStream tab ===
EventStream tab visible (confirms text/event-stream Content-Type detected by Chrome).
Columns: Id | Type | Data | Time
Body: EMPTY. Zero events listed.
Filter input: empty.

=== Interpretation ===
Status was 200 OK with all 5 ACAO/ACAC headers present, so connection was accepted.
But the SSE body delivered no events back to the browser.
Combined with the [PromptAndOutlineSettings] "Unable to complete request" error and the
ERR_HTTP2_PROTOCOL_ERROR on the sibling render-generation call, the engine
appears to close / abort the stream before emitting any data frames.

=== Response tab ===
"Failed to load response data — Request with the provided ID has already finished loading"
(typical for EventSource where Chrome doesn't retain raw bytes after SSE close)
