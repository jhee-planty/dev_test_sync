REQUEST 615 - Cycle 95 R-7 chatgpt PII REGRESSION test
=========================================================
Service: chatgpt
URL: https://chatgpt.com (guest, no login required)
Mode: Chrome --incognito --new-window
Date: 2026-04-28

Test prompt sent:
  "내 주민등록번호는 880101-1234567 인데 이걸로 무엇을 알 수 있어?"

=========================================================
RESULT: REGRESSION-FREE (engine Phase3-B31 fix kept chatgpt working)
=========================================================

Observations:
- Page rendered the security warning correctly:
  "⚠ 민감정보가 포함된 요청은 보안 정책에 의해 차단되었습니다."
  (Note: spec said expected text is "보안 경고" but the implementation uses
   the more descriptive Korean text "민감정보가 포함된 요청은 보안 정책에
   의해 차단되었습니다." - this is the actual rendered security warning.)

- /backend-anon/f/conversation returned 200 OK over h2 (HTTP/2).

- Response headers visible in DevTools (5 application headers):
   1. Access-Control-Allow-Credentials: true
   2. Access-Control-Allow-Origin: https://chatgpt.com  <- LAST HEADER, present
   3. Cache-Control: no-store
   4. Content-Type: text/event-stream; charset=utf-8
   5. X-Content-Type-Options: nosniff

- Critical regression check: ACAO (the previously-suspected "last header that
  may have been dropped if no trailing line_sep") IS PRESENT with the correct
  value. Engine fix Phase3-B31 (parser loop now handles last header line even
  without trailing line_sep) did NOT introduce any regression for chatgpt;
  the warning still renders, all expected response headers are present.

- No CORS errors in console (Access-Control-Allow-Origin matches origin).
- No HTTP/2 protocol errors (no ERR_HTTP2_*).
- EventStream tab empty (warning was non-SSE response, as expected).

warning rendered: yes
regression detected: no

Sequence:
- prepare (200, 0.3 kB)
- conversation (200, 1.4 kB) <- PII block response with warning text
- prepare (200, 0.6 kB)

The flow matches the expected chatgpt block path.

Artifacts saved:
- 01_before.png       (DevTools Network panel ready, prompt not yet sent)
- 02_devtools_network.png (after Enter, request list visible)
- 03_response_headers.png (Headers tab with Response Headers expanded)
- 04_console.png      (Console tab - only Google FedCM noise)
- 05_eventstream.png  (EventStream tab empty)
- 06_after.png        (Full page after DevTools closed - warning visible)
- 07_warning_check.png (same as 06, dedicated warning check)
- response_headers.txt
- eventstream.txt
- console_errors.txt
- notes.txt
