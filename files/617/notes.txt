Cycle 95 R-10 — zeta verify after engine Phase3-B31 last-line parser fix.

Test flow:
- Opened Chrome incognito → https://zeta-ai.io
- Dismissed consent dialog ("동의하기")
- Picked first character on home (특수부대, 상급자들. — plot 02e6ad74-...)
- Clicked "대화 시작하기" → entered chat room 0cc65e4c-d2c5-4fda-acbf-9a3f07badd1a
- F12 → DevTools opened (Network + Console used; Preserve log was already ON)
- Filter set to "stream"
- Pasted PII prompt and sent (clicked send button)
- Dismissed app-promo modal that appeared mid-flow
- Inspected the failed /messages/stream request

Key observations:
- warning rendered: NO (no chat-bubble warning text appeared on page)
- error popup: NO (no "일시적인 오류가 발생했어요" toast either — just silently nothing visible)
- The user's prompt itself was NOT rendered as a user bubble, and no AI reply bubble appeared. UI silently swallowed the failure.
- Network panel: 2 stream requests visible (1 OPTIONS preflight 200, 1 POST main shown as "(failed)")
- Headers tab confirmed Status 200 OK + 5 response headers including Access-Control-Expose-Headers: *
- EventStream tab confirmed 2 SSE events delivered (IN_PROGRESS + CHAT_COMPLETE) before connection close
- Console showed `net::ERR_CONNECTION_CLOSED 200 (OK)` — IDENTICAL fingerprint to #607 / #612

Engine fix evaluation (Phase3-B31 last-line parser):
- POSITIVE: Access-Control-Expose-Headers (the LAST header in zeta_sse_v2 template) IS PRESENT and correctly emitted. The last-line parser fix worked for header preservation.
- POSITIVE vs prior cycles: 2 SSE events were actually delivered to the client. #607/#612 had 0 events. So the engine is now sending body frames before the close.
- NEGATIVE: Connection still RST'd / closed before clean SSE termination → Chrome marks request "(failed) ERR_CONNECTION_CLOSED" → frontend EventSource discards the stream → no UI render. The h2_end_stream=2 / connection-shutdown root cause from #607/#612 is NOT resolved by this fix.

Progress vs #607/#612: BETTER (we now have 2 events delivered + headers intact) but still FAILED at user-visible level (no warning rendered, no popup, complete UX silence).

Root cause hypothesis:
- Phase3-B31 fixed the last-header drop, but the underlying H2 stream-close timing bug remains. The engine is closing the H2 stream too early or with the wrong end_stream/RST_STREAM flag (h2_end_stream=2 = RST instead of clean END_STREAM). Browser sees: 200 + headers + partial body + RST → treats whole response as net::ERR_CONNECTION_CLOSED → fetch reader rejects → EventSource error handler fires but app code in zeta-web has no path to render the partial text it never received in onmessage.
- Also: ERR_HTTP2_PROTOCOL_ERROR on chat-model-configs and feature-flags/web-purchase suggests the H2 connection itself is unhealthy across multiple endpoints, not just /messages/stream — could be a connection-level (not per-stream) framing/flow-control issue introduced/unfixed by the deploy.

Recommended next step:
- Investigate the engine's H2 frame emission order specifically the moment after the final SSE chunk; ensure END_STREAM is set on the last DATA frame (or a final empty DATA with END_STREAM), and that no RST_STREAM is sent. Look at h2_end_stream=2 logic in the SSE responder.
