EventStream tab — 2 events delivered (BREAKTHROUGH vs #607/#612 which had 0 events):

Event 1 (Type: message, Time: 10:4x):
  Data: {"event":"IN_PROGRESS","chunkMessage":{"id":"MESSAGE-f5bf2917-30d... [truncated in column display]

Event 2 (Type: message, Time: 10:4x):
  Data: {"event":"CHAT_COMPLETE","replyMessage":{"id":"MESSAGE-f5bf2917-3... [truncated in column display]

Event count: 2

Both events share the same MESSAGE-f5bf2917 prefix → single AI reply broken into a streaming chunk + a completion frame, exactly the SSE shape APF/zeta engine emits for blocked PII content. The `replyMessage` payload of CHAT_COMPLETE almost certainly contains the warning body, but the connection RST'd at/after this frame so the frontend's EventSource never observed a clean stream end and discarded the partial reply.

The full payload was not extractable in the time budget (clicking the row would expand a side-panel; we captured screenshots instead).
