Request #665 — character.ai F5 step 3-B WS masking unwrap fix verify (RETRY of #664)
Engine build: run_id=20260430-145635-59395
Test execution: 2026-04-30 15:02 KST
Test prompt: 내 주민등록번호는 880101-1234567 인데 이걸로 무엇을 알 수 있어?

============================================
FINAL URL
============================================
https://character.ai/chat/Ks8_tz2ARBEL09GXrN8PG6I-9rIbh_2C5qsEn_Vv2r4

============================================
CONNECTION STATE AFTER PROMPT
============================================
WS REMAINED OPEN throughout the entire LLM streaming response.
NO ws_blocked event. NO connection close. NO disconnect.
The new turn went through identically to a normal LLM exchange.

Console evidence (after prompt send, fresh log):
- "Sending socket message {command: 'create_and_generate_turn'}"
- "WS Message Received for create_and_generate_turn add_turn"  (x2)
- "[useSentryMeasurement] FirstChunkLatency streaming took 1171741ms"
- "WS Message Received for create_and_generate_turn update_turn"  (x18+ — full stream)

NO occurrences of any of the engine-close indicators:
- No "WS Closed"
- No "CloseEvent {code: 1006...}"
- No "Retrying, retry attempt: N"
- No "Core Websocket Opened" (re-open after close)

Comparison: When opening DevTools at session start (before this test), the
console DID show prior #664-era WS Closed/code 1006/Retrying events. After
the fresh prompt for #665, none of those signals reappeared — meaning the
engine did NOT intercept the masked WS frame this time either.

============================================
UI STATE
============================================
LLM produced a full multi-paragraph in-character roleplay response that
explicitly analyzes the SSN as identifying personal data.

Captured response excerpts (Adam McLeod replying to "880101-1234567"):
- "(digging through a moldy first aid kit) 'You're clinging to ghosts.
  That number meant vaccines and tax returns back when hospitals still
  had electricity.'"
- "'Now it just tells me you're older than the quarantine zones — which
  means you should know better than to shout personal details with Virals
  nesting in the walls.'"
- "(whispering urgently) 'Here's what your number actually tells us — you
  survived the initial outbreak when 90% didn't. You remember real coffee.
  And right now...'"

NO warning bubble. NO error message. NO blank/empty response. NO refusal.
The character explicitly references the meaning of the ID number (vaccines,
tax returns, age inference, "older than quarantine zones") — i.e. it IS
performing PII analysis on the user-supplied SSN, in roleplay form.

============================================
WEBSOCKET EVIDENCE
============================================
Network → WS filter at start of session showed 3 wss://neo.character.ai/ws/
entries (carryover from #664). After clearing log + sending the #665 prompt
+ socket-filter view: 0/9 socket-typed network rows. The active WS
connection was reused (not torn down), and the engine did NOT close it.

No close frame observed. No abrupt termination. No code 1006.
Streaming session lifetime ~ duration of the LLM reply (full delivery).

============================================
LLM RESPONSE CHECK
============================================
- Real character LLM text: YES (multi-paragraph)
- PII analysis present: YES (explicit references to what the number "tells
  us" — vaccines, tax returns, age, identity)
- Analogous to #664's "Born winter 1988, right? Means you'd be... what, 82
  now if the world hadn't ended?"
- The fix's intended block: NOT triggered

============================================
FINAL VERDICT: REGRESSION (mission-critical)
============================================
The masking-unwrap fix in build run_id=20260430-145635-59395 did NOT close
the WS connection and did NOT prevent the real character LLM from streaming
PII analysis of the user-supplied SSN.

Same engine-bypass behavior as #664. ws_blocked still does not fire on
character.ai SSN PII messages. Engine still misses the masked outbound
frame containing the SSN keyword.

Hypotheses (for next iteration):
1. The RFC 6455 frame parse may be running, but mask-XOR may still not be
   applied to the payload before the keyword scanner inspects it.
2. character.ai's payload is a JSON envelope; the SSN may be inside a
   nested string field that the keyword scanner does not reach even after
   unmasking (e.g. needs JSON-aware scanning, not raw bytes scan).
3. Possible: the scanner runs on the wrong side (server->client direction
   instead of client->server), and outbound user messages still bypass.
4. Possible: TLS still wrapping prevents plaintext frame inspection
   (would explain why prior services like Le Chat fire ws_blocked but
   character does not — character may use a different TLS handshake or
   ALPN that the engine's TLS terminator misses).

Artifacts saved to C:\Users\최장희\Documents\dev_test_sync\files\665\:
- initial_state_from_664.png — pre-test (shows old #664 close events)
- before.png — prompt typed, not yet sent
- after_t8s.png — 8s after Enter (LLM mid-stream, full PII analysis visible)
- network.png — Socket filter view, 0/9 socket rows (no new WS opened, no close)
- console.png — clean console after #665 send: only normal create_and_generate_turn / add_turn / update_turn flow, no WS Closed
- notes.txt — this file
