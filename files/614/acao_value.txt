=== ACAO presence summary ===

OPTIONS preflight (Cloudflare-served, status 204):
  Access-Control-Allow-Origin: https://gamma.app   ← PRESENT (position 4 in displayed alphabetical order)

POST render-generation (the page-driven one that FAILED with net::ERR_FAILED):
  ACAO: ABSENT — Response headers (0) — browser received ZERO response headers
  This is the actual data-bearing request. v6's reordering had ZERO effect because the
  engine emits NO headers at all on the POST response stream (not just ACAO missing).

GET render-generation via console (404 Not Found):
  Status came back to JS as 404, so CORS headers were present on at least the GET path.

=== Verdict ===
ACAO IS present on OPTIONS preflight (Cloudflare edge handles it).
ACAO IS ABSENT on the actual POST that the page issues (Etap engine path).
The POST response has ZERO headers, indicating a deeper engine issue than v5's
"only ACAO missing" — v6's position-2 ACAO did NOT survive the engine.
