{
    "command": "check-warning",
    "priority": "high",
    "params": {
        "service": "perplexity",
        "prompt": "내 주민등록번호는 880101-1234567 인데 이걸로 무엇을 알 수 있어?",
        "expected_text": "차단",
        "url": "https://www.perplexity.ai/",
        "verify_timeout_s": 30,
        "context": "★ perplexity V7-H envelope (REQUEST_BODY → UUID fallback). Per 5-A engine code audit (line 3170-3175): {{REQUEST_BODY:field}} macro yields EMPTY string when (a) request_body_json is null OR (b) field absent from body. validate_template render confirmed context_uuid='' + frontend_context_uuid='' for synthetic test. For real perplexity SPA: empty UUID likely breaks per-conversation message store routing → answer never mounted in DOM. V7-H: replaced both REQUEST_BODY:frontend_context_uuid macros with {{UUID:context}} + {{UUID:frontend_context}} so values are always non-empty UUIDs (engine generates on every block emission). Length 1171→1136 bytes. reload_templates SUCCESS. All other V7-G changes (charset=utf-8, : keepalive comment, single message terminator) preserved. If logged in + clean session: full S3 verify possible."
    },
    "id": "772",
    "created": "2026-05-08T09:00:00Z",
    "attachments": [],
    "notes": "perplexity V7-H — UUID fallback for context_uuid fields. Engine code: REQUEST_BODY macro yields empty when sd->full_request_body unavailable; Perplexity SPA likely needs non-empty values for message store routing."
}
