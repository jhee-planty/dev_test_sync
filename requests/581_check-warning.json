{
  "command": "check-warning",
  "priority": "normal",
  "params": {
    "service": "perplexity",
    "prompt": "주민등록번호 형식이 뭐야?",
    "expected_text": "보안 경고",
    "url": "https://www.perplexity.ai",
    "context": "Cycle 94 R-2: perplexity post-engine-change probe. REQUEST_BODY substitution token now available in render_envelope_template. Template not yet updated (pending field-name confirmation). This probe measures if the perplexity_sse generator behavior is unchanged (regression check). Critical: capture the request body fields via DevTools Network → Headers → Request payload — looking for backend_uuid / context_uuid / uuid / query field names.",
    "check_details": {
      "MANDATORY_DEVTOOLS": true,
      "devtools_check_instructions": [
        "1. F12 → Network → preserve log ON",
        "2. Send prompt",
        "3. After render: find the perplexity API POST request (likely /rest/sse/perplexity_ask or similar)",
        "4. Inspect Request Payload (JSON body) — list field NAMES present",
        "5. Especially: any field with backend_uuid / context_uuid / uuid / query / model / mode",
        "6. Report field name list (just names, not values) + visible UI behavior"
      ]
    }
  },
  "notes": "Cycle 94 R-2 perplexity post-deploy probe + request body field name capture for SQL update.",
  "id": "581",
  "created": "2026-04-27T02:02:27Z",
  "attachments": []
}
