{
  "id": "130",
  "command": "run-scenario",
  "priority": "normal",
  "params": {
    "description": "Frontend inspect: Grok",
    "steps": [
      { "action": "open", "url": "https://grok.com" },
      { "action": "observe", "check": "page loaded" },
      { "action": "screenshot", "name": "baseline.png" },
      { "action": "send-prompt", "text": "Hello" },
      { "action": "observe", "check": "response rendered" },
      { "action": "screenshot", "name": "response.png" },
      { "action": "analyze", "check": "Content-Type (expect application/json NDJSON), framework, response DOM" }
    ],
    "collect": ["page URL", "HTTP headers", "Content-Type", "framework", "response area", "console errors"]
  },
  "created": "2026-04-01T14:04:00",
  "notes": "Phase 1 frontend inspect — Grok. NDJSON format expected (not SSE)."
}
