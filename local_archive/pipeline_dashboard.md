# APF Pipeline Dashboard

> Last updated: **2026-04-16 10:48 KST** · Mode: `autonomous_loop_polling` (**DEV_DRIVES**) · Loop interval: on-demand
> Source of truth: `local_archive/pipeline_state.json`

## Current focus

| Field | Value |
|-------|-------|
| Service | **github_copilot** → DONE ✅ · **deepseek** (phase6_test_fail) · **huggingface** (phase6_test_fail) · **v0** (needs_user_session) · **gemini3** (phase5_schema_debug_required) |
| Phase | **phase6_verified_partial** — 1/4 SUCCESS, 2/4 FAIL, 1/4 NEEDS_LOGIN |
| Strategy | github_copilot SSE injection works. deepseek + huggingface need envelope schema debug (capture success response → diff → fix). v0 needs authenticated session re-test. |
| Pending requests | **#456** — deepseek (fragments fix) + huggingface (conversationId fix) retest |
| Last result processed | **#455 check-warning 4-service** — github_copilot WARNING_DISPLAYED, deepseek BLOCK_ONLY (network error), huggingface BLOCK_ONLY (silent fail), v0 NEEDS_LOGIN |
| Blockers | deepseek/huggingface: envelope schema mismatch. v0: sign-in policy change. gamma: PENDING_INFRA. |

## Recent events

| Time (KST) | Event |
|------------|-------|
| 13:10 | #447 frontend-inspect-v0 pushed to test PC |
| 13:32 | Loop polling setup — full cowork-remote schema, 7-service queue |
| 13:42 | #447 result arrived (COMPLETED, 32 min turnaround) |
| 13:43 | Phase 4 deliverable written: `services/v0_frontend.md` |
| 13:44 | Key finding: **no native error UI** → Option E becomes primary (not Option A) |
| 13:45 | Phase 5 skeleton revised, pipeline_state → phase5_design |
| 13:49 | v0_frontend.md committed (fb5f82e) + pushed to dev_test_sync |
| 13:50 | Blocker #3 resolved: `render_envelope_template()` is content-type agnostic — Option E is pure DB migration |
| 14:10 | Cycle 2: no new results. Blockers #1, #2 resolved via code reading (domain_patterns comma-delimited, path_matcher prefix by default). v0_design.md promoted to tracked services/ folder. |
| 14:15 | Cycle 3: #448 v0 navigation mode probe pushed (Option E pre-flight, anonymous, HIGH). Rate Limit Gate: 1/2. |
| 14:20 | Cycle 4: #448 still pending (2nd empty poll). Re-read cowork-remote + phase5-warning-design + warning-delivery-checklist skills to prep for eventual Phase 5 re-design via Claude Code sub-agent. |
| 14:25 | Cycle 5: #448 still pending (3rd empty poll, stall=2). Dev-side code reading: confirmed dispatcher is pure DB lookup (ai_prompt_filter.cpp:1254-1270). Added INSERT fallback to v0_design.md for defensive Phase 6 safety. |
| 14:30 | **User prompt**: "테스트 서버의 로그는 확인하고 있어?" → SSH etap log check performed. **DEFINITIVE VERDICT**: all v0 Page load requests are either `v0.app/` root or Kasada `/fp?x-kpsdk-v=...` — NO `/chat/<id>` ever logged. Option E BLOCKED. v0_design.md pivoted to Option F (303 redirect) as Phase 6 primary experiment. |
| 14:32 | #448 result arrived. Confirmed `is_document_request: false` (etap log verdict validated). Additional insights: fetch()-doesn't-navigate-on-303 caveat exposed → Option F low-yield; introduces Option G (403+meta-refresh body) and Option H (reload-case /chat/<id> document intercept). Revised path: **f+h pair Phase 6 experiment** → g fallback → NEEDS_USER_SESSION for Option A. |
| 14:37 | **Cycle 7 — gamma pivot.** v0 Phase 6 blocked on DB access. Per parallel plan, switched focus to gamma (priority 2). L2 SSH check confirmed envelope IS shipping (`response_type=gamma_sse, size=333, http2=1, h2_end_stream=1`) — contradicts existing `gamma_design.md` claim of H2 DATA delivery failure. Real issue: non-chat consumption pattern. Drafted + pushed **#449** (gamma frontend visibility probe, 99a808a): JS textarea bypass via `HTMLTextAreaElement.prototype` setter + SSR/CSR navigation check + DOM consumption mode classification. Rate Limit Gate: 1/2. |
| 14:50 | Cycle 8 — empty poll. #449 in flight ~13min (within 8-10min envelope + tolerance). L2 SSH: no gamma block in etap log since 09:07 — test PC has not yet executed submit step. Likely still in login check / baseline / JS bypass phase. Heartbeat stale (test PC pauses heartbeat updates during active scenarios). Next cycle 9 at 14:54 will reassess. |
| 14:55 | **Cycle 9 — #449 result + PARADIGM SHIFT.** Test PC found gamma `/create/generate` renders BLANK (2886-byte HTML shell, zero body content, 60 script tags but no `#__next` div). 59 console errors with `ERR_HTTP2_PROTOCOL_ERROR` on `_ssgManifest.js` and `_buildManifest.js`. Test PC theorized "APF blocks Next.js bootstrap chunks via over-broad path rule." **L2 SSH cross-check CONTRADICTS:** (a) ZERO gamma traffic in etap.log during the entire #449 window — APF saw nothing from gamma.app, so it cannot have blocked anything; (b) burst of **38 `SetCertificate failed` errors in `visible_tls/tls_proxy.cpp:900`** at 14:34:36–47 (11s window). Hypothesis pivot: TLS interception cert chain failure for gamma.app, dropping the connection at the visible_tls layer, surfacing as ERR_HTTP2_PROTOCOL_ERROR in Chrome. Marked `gamma_design.md` STALE with detailed update note. #158's "SendKeys can't paste into textarea" was retroactively the same blank-page failure (no textarea ever existed). |
| 14:58 | **Cycle 9 cont. — #450 dispatched.** Drafted + pushed gamma HAR forensics probe (commit 46eaf0e) — passive observation only, no submission, no JS bypass, no blocked keywords. Captures network-level evidence (Status, Headers, Response body, Timing, full HAR, Console errors) for `_ssgManifest.js` and `_buildManifest.js` to definitively classify failure mode: APF block (333B envelope) / TLS handshake fail / TCP reset / Cloudflare bot challenge / empty 200 / 5xx. Rate Limit Gate: 1/2. |
| 15:10 | **Cycle 10 — code-side investigation while #450 in flight.** Read `functions/visible_tls/tls_proxy.cpp:900` (SetCertificate failure site) + `functions/visible_tls/crt_manager.cpp:1379-1402`. Found "smoking gun" comment by `nr.` 2024/7/29: `_kx_nid = NID_kx_rsa; _auth_nid = NID_auth_rsa;` hardcoded with unfixed aliexpress.com regression note. Hypothesized: Cloudflare-fronted gamma.app uses ECDSA cert → RSA forcing fails. Documented 4 action options (A: SNI bypass / B: crt_manager enhancement / C: per-host override / D: status quo) in local_archive/gamma_blank_page_analysis_2026-04-14.md. |
| 15:15 | **Cycle 11 — #450 HAR arrived, BOTH hypotheses REFUTED.** Processed #450 result (69 entries, 8 success / 61 stalled). Key findings: (1) Two fresh TLS handshakes to gamma.app succeeded in one page load, including for the icon fetch AFTER the manifest stall window — TLS path is FINE. (2) `/manifest.json` returned 200 on the SAME H2 connection while 61 `/_next/static/*` requests were mid-stall — the connection is healthy for some paths, stalled for others. (3) `sudo mysql etap` query against `ai_prompt_services` confirms NO rule whose domain_patterns matches host `gamma.app` (gamma row is `ai.api.gamma.app` exact match per `domain_matcher::match` at `ai_prompt_filter_db_config_loader.cpp:72-124`). (4) Running etap binary emits `[APF] Page load request` from `ai_prompt_filter.cpp:697` — that string does NOT exist in ANY branch of this worktree. Source tree drift confirmed; cannot predict running-binary behavior from worktree code-reading. (5) `/var/log/etap.log` stopped appending at 14:59:28.860 — process (PID 3697568) alive but logger silent through entire #450 window. L2 monitoring blind spot. |
| 15:20 | **Cycle 11 cont. — gamma → PENDING_INFRA, pivot to deepseek.** Updated `local_archive/gamma_blank_page_analysis_2026-04-14.md` with full cycle 11 reversal section. Updated `gamma_design.md` STALE marker: explicit resume criteria (user inspects running-binary source, restarts etap to clear stuck logger, infra Cloudflare/visible_tls investigation). Updated `pipeline_state.json`: `current_service=deepseek`, gamma=`pending_infra`, added `monitoring.etap_log_writer_suspected_stuck_since=14:59:28`. Per autonomy directive, pivoting to priority 3 (deepseek) for Phase 1 HAR capture rather than blocking on gamma. |
| **2026-04-16** | | |
| 10:48 | **Cycle 62 — #455 RESULT PROCESSED. 1/4 SUCCESS.** github_copilot → **DONE** (경고 텍스트 '⚠ 민감정보가 포함된 요청은 보안 정책에 의해 차단되었습니다.' 정상 렌더링, thumbs/copy/retry 버튼 포함). deepseek → **FAIL** (generic network error '네트워크를 확인하고 다시 시도하세요' — APF 경고 아님). huggingface → **FAIL** (complete silent fail, conversation UUID 생성되었으나 버블 0개). v0 → **NEEDS_LOGIN** (sign-in 필수 정책 변경). DONE 서비스 6→7. Loop polling 체인 끊김 발견 — one-shot 태스크 10:15 실행 후 후속 poll 미스케줄. |
| 09:57 | **Cycle 61 — Context recovery + state update. Phase 6 DB migration CONFIRMED in production** (etap log shows reload at 09:48-09:49: 29 services, 40 response templates, 36 envelope templates). DB audit: all 4 templates verified (deepseek_sse 454B, copilot_sse 586B, huggingface_ndjson 282B, v0_html_block_page 124B). **v0_api gap confirmed**: v0_api service row and v0_303_redirect template NOT created (PART 1B.1b/2b skipped in migration). Only v0 html_block_page active. Not blocking #455 test (tests v0_html_block_page only). pipeline_state promoted to phase6_testing, poll scheduled at 10:15. |
| 09:30 | **Cycle 60 cont. — #454 HUGGINGFACE WINNING RESULT PROCESSED. Phase 4+5 dual delivery. ALL 4 SERVICES NOW READY.** #454 arrived 08:45 KST (~15h from push). **NDJSON format (application/jsonl)** — NOT SSE. 8 event types: conversationId_init, status:started, status:keepAlive, routerMetadata, stream token (with null-byte padding), finalAnswer, status:finished, [DONE]. **Silent fail confirmed** (zero error UI — identical to Gemini). **CSP extremely permissive** (only frame-ancestors). **Option A NDJSON injection HIGHLY VIABLE** — easiest service of all inspected. Option D also HIGHLY VIABLE (no CSP). response_type renamed `huggingface_sse` → `huggingface_ndjson` (follows grok_ndjson/notion_ndjson convention). Content-Type=`application/jsonl`, separator=`\n`. Wrote **huggingface_frontend.md** (FRONTEND_PROFILED) + promoted **huggingface_design.md** (DESIGN_LOCKED). **All 4 Phase 6 services now READY**: deepseek + v0 + github_copilot + huggingface. Combined DB migration ~25-35min. Gate: DB access to 218.232.120.58. |
| 09:14 | **Cycle 60 — Envelope audit §22: READINESS SUMMARY AND CONCLUSION. GO for Phase 6.** Empty poll #34 (#454 absent ~+395min). Executed candidate (kk): synthesized §1-§21 into Phase 6/7 readiness statement. **No blocking defects found across 22 sections spanning cycles 34-60.** All severity=MEDIUM items mitigated (F1 DELETE-then-INSERT, F3 m365 CORS fixed). Per-service: deepseek READY, github_copilot READY, v0 READY, huggingface CONDITIONALLY READY (awaiting #454). Phase 7 has 2 mechanical prereqs: (1) commit 574-line worktree diff, (2) 15-tag contamination rename. Combined Phase 6 migration plan: 4 services × ~5min = 25-35min DB window. §22.8 section index provides quick-reference for all 22 audit sections. **This concludes the envelope audit series** — all APF hot-path modules fully audited. Remaining dev-side candidates: (ii) B1-B30 build tag inventory, (jj) notion response-body trace. Audit series complete — may reduce polling frequency until #454 or DB access arrives. |
| **2026-04-15** | | |
| 22:48 | **Cycle 58 — SQL Part 3 full cross-check; chatgpt MESSAGE_RAW latent risk found; HF Phase 6 envelope sketch drafted.** Empty poll #32 (#454 absent +310min, **5.0x #453 precedent**, HF flat ~152min). Candidate (gg) executed: `sql/apf_db_driven_migration.sql` Part 3 (cpp:67-299) — 10 response_types × 12 service entries. **Coverage matrix**: chatgpt (chatgpt_prepare+chatgpt_sse), claude, github_copilot (copilot_403), grok (grok_ndjson), m365_copilot (m365_copilot_sse), gamma (gamma_sse), notion (notion_ndjson), perplexity (perplexity_sse, shared by perfle), genspark (genspark_sse), gemini (gemini, shared by gemini3). **HF ABSENT confirmed** — Part 3 has no huggingface entry; must be added in phase6_huggingface_addendum_*.sql. **Placeholder rules verified**: MESSAGE (json_escape) 9 services, MESSAGE_RAW chatgpt 2 sites, UUID/TIMESTAMP/ESCAPE2/BODY_INNER_LENGTH all accounted for, no stray TODO/TBD/placeholder literals. **MAJOR FINDING — chatgpt MESSAGE_RAW in JSON-string value is LATENT**: both chatgpt_prepare (cpp:83 `"error":"{{MESSAGE_RAW}}"`) and chatgpt_sse (cpp:104 `"v":"{{MESSAGE_RAW}}"`) place MESSAGE_RAW inside a JSON string value. If http_response text ever contains `"` or `\` or control chars, JSON is malformed and chatgpt client fails to parse. Current canonical texts (priority=50 한영 병기, priority=1 한글 단문) are safe (no special chars) so DONE verdict not invalidated, but any future edit with quotes breaks silently. Cycle 47 http_response guidance warns about BLOCK/0/NULL placeholder tokens but NOT JSON-special chars. Recommendation: convert chatgpt to `{{MESSAGE}}` (json_escape) in future migration — byte-identical for canonical texts, safe for edits. **Notion h2_mode=2 + h2_hold_request=0 ANOMALY**: every other h2_mode=2 service uses hold_request=1 (request-body detection); notion uniquely uses hold_request=0 (response-body detection). Legitimate per §17 but unique mode — notion inspects server response not client request. Cycles before never traced notion-specific response-body path. **Content-Length: 0 consistent** across all 11 envelopes (APF recalculates per §9). **SSE separator split**: perplexity_sse (v5 LOCKED) and genspark_sse (JS parser constraint) use `\n\n` — others use `\r\n\r\n`. B27 separator picker handles automatically. **h2_end_stream distribution**: 0=3 services, 1=9 services, **2=0 services (unused in baseline)** — Phase3-B30 delayed END_STREAM code path exists but no service uses it; HF Phase 6 could be first if #454 requires. **Stale comment** at cpp:156 `BLOCKED_ONLY 판정` for grok contradicts DONE status. **HF Phase 6 envelope sketch drafted (§20.11)**: response_type=`huggingface_sse`, prepare_response_type=`''`, h2_mode=`2`, h2_end_stream=`1`, h2_goaway=`0`, h2_hold_request=`1` (matches grok/copilot/gamma), MESSAGE form `{{MESSAGE}}`. Needs from #454: SSE event schema, separator, delta vs finalAnswer, CSP/injection viability. **Verification count STAYS 15** (§20 is data audit not runtime verification). Wrote envelope_audit §20 (sections 20.1-20.13). Commit pending. Next cycle 59 ~22:51 plan: (jj) trace notion response-body detection path. |
| 22:43 | **Cycle 57 — sensitive_keyword_matcher audit (last uncovered APF hot-path module); priority semantics found UNEVEN.** Empty poll #31 (#454 absent +305min, **5.0x #453 precedent**, HF flat ~147min). Candidate (ff) executed: audit `sensitive_keyword_matcher.cpp` (449L) + `.h` (249L) — this is the actual filename, cycle 56's "ai_prompt_keyword.cpp" was wrong. **Architecture**: lock-free `matcher_bundle` via `etap::instance_switcher<shared_ptr<matcher_bundle>>`; rebuild builds new bundle + atomic swap; readers take shared_ptr copy so concurrent rebuild is safe. **Match entry (cpp:64-97)**: 2-phase — unified AC (EXACT+PARTIAL, case-insensitive) first, then RE2 cache; first-match-wins across both phases. **Post-processing**: EXACT enforces word-boundary before/after; PARTIAL skips boundaries. **Korean-friendly word boundary (cpp:297-362)**: multi-byte chars always treated as boundary → particles like '주민등록번호**가**' still match. **REGEX (cpp:422-448)**: linear RE2::PartialMatch over cache sorted by priority desc; first hit wins. `result.position` NOT populated for regex matches (only for AC at cpp:413) — latent if future diagnostics read it. **MAJOR FINDING — PRIORITY SEMANTICS UNEVEN**: `keyword_metadata::priority` copied at cpp:181 but **never read during AC matching**. Priority ONLY affects REGEX cache sort order. For AC keywords, match order is determined by Aho-Corasick positional order not priority. Comment at cpp:86 '우선순위 순으로 검사 (이미 정렬되어 있음)' is misleading — true only for regex. Two overlapping AC keywords will match by AC traversal not priority. Not a correctness bug for HF but authoring pitfall. **B17 space-normalization fallback** discovered at apf.cpp:2448-2467 — caller layer, NOT matcher layer; if miss, strip spaces/tabs + retry; handles IME-inserted '주 민 등 록 번 호'. Early-exit on success so block path has no cost. **B17 was missing from §17's B13-B30 cross-ref list** — now added to inventory. **HF Phase 6 relevance**: matcher is service-agnostic — no HF-specific keyword subset. BLOCK_VERIFIED from cycle 36+ covered detection; §19 confirms no service gating exists. **LAST uncovered APF hot-path module NOW code-audited**: ai_prompt_filter.cpp ✅ / db_config_loader ✅ / sensitive_keyword_matcher ✅. Remaining: apf_filter_config.cpp (XML, non-hot-path, deferred) + sql Part 3 (data, cycle 58). **Side observations** (cycle 58+): (1) unused priority field, (2) misleading cpp:86 comment, (3) B17 missing from §17.9, (4) regex position not populated, (5/6) KEYWORD_CHECK + KEYWORD_NORMALIZED non-namespaced logs → adds 2 more rename sites to §18 Phase 7 cleanup (now 23 total). **Verification level (o) added**: 'Keyword matching engine (AC+RE2, lock-free bundle swap, word-boundary, B17 fallback)'. Count: **15 (a-o)**. Wrote envelope_audit §19 (sections 19.1-19.10). Commit pending. Next cycle 58 ~22:46 plan: (gg) sql Part 3 envelope template cross-check against cycle 48 placeholder rules. |
| 22:38 | **Cycle 56 — Full [APF:] log tag inventory; 20 legitimate + 6 legacy + 15 contaminated; Phase 7 gate replacement drafted; cycle 11 source-drift RE-CONFIRMED.** Empty poll #30 (#454 absent +300min, **5.0x #453 precedent**, HF flat ~142min). Candidate (ee) executed: grepped `[APF:` + `"[APF]` across ai_prompt_filter.cpp + visible_tls_session.cpp. **20 legitimate tags** with 17 unique subcategories (validate×4, hold_set_h1/skip_h1/release_h1, hold_set/skip, v0_diag, block, block_response, block_session_h2, H2_SSE/DECHUNK/STATUS/HPACK, envelope, h2_params, delayed_ES). **6 legacy `[APF]` raw-format logs** at apf.cpp:569/586/722/859/1611/1640 with rename suggestions (pageload_h1/ws_upgrade_h1/pageload_h2/ws_upgrade_ai/no_template/no_envelope). **15 contamination rename targets drafted** (§17.7 sites → non-colliding `[APF:vts_*]` subcategories). **Phase 7 release-gate replacement**: AND-gate (no `[APF_WARNING_TEST:]` in functions/ + no APF_WARNING_TEST token outside docs). Cycle 52's spawned side task scope must expand from 2 to 15 lines. **Cycle 11 source-drift RE-CONFIRMED (strengthened)**: `git log --all -S "WebSocket upgrade detected for AI service" -- apf.cpp` returned EMPTY, git status shows file modified+unstaged, git diff shows cpp:586/859 as `+` additions → my worktree has uncommitted local adds on top of HEAD. Running test-server binary was built from HEAD (not this dirty worktree), so cycle 11's "running binary emits strings not in ANY worktree branch" claim remains valid for the test server. My cycle 55 confusion (thinking cpp:859 was canonical) came from reading the dirty worktree. HEAD is still clean of both strings. Gamma PENDING_INFRA verdict unchanged. **Side-effect flagged**: apf.cpp worktree has ≥2 uncommitted local additions; cycle 57+ housekeeping should inventory full git diff for other stray adds before Phase 7. **Observations**: `[APF:v0_diag]` at cpp:782 is v0-specific post-cycle-38 diagnostic — dead code candidate after v0 Phase 6 ships; `[APF:validate]`×4 only acceptable subcategory reuse; **missing observability** — no info log at `detect_and_mark_ai_service` entry (only `[APF:block]` on block path, no "service detected" trace for hold+no-keyword paths). Wrote envelope_audit §18 (sections 18.1-18.7): full tag tables, rename mapping, Phase 7 gate replacement, cycle 11 follow-up. Verification count STAYS at **14** (tooling audit, not runtime verification). Commit pending. Next cycle 57 ~22:41 plan: (ff) ai_prompt_keyword.cpp audit — last uncovered major APF module. |
| 22:32 | **Cycle 55 — VTS 3-module pipeline fully traced (ai_prompt_filter → network_loop → visible_tls_session); 13 MORE test-log contamination tags found in visible_tls_session.** Empty poll #29 (#454 still absent at +295min, **5.0x #453 precedent**, HF flat ~137min). Executed cycle 54 plan candidate (dd): audit visible_tls_session.cpp + network_loop.cpp to trace where `_apf_hold_for_inspection` and `_block_response_string` are actually consumed outside ai_prompt_filter module. Read visible_tls_session.cpp:507-772 + network_loop.cpp:1220-1257 + etap_packet.h:1335-1341 + tuple.h:743-744. **3-MODULE ARCHITECTURE DOCUMENTED** — never fully traced in one place before: **(1) ai_prompt_filter** sets `_session._apf_hold_for_inspection` (sticky), `_apf_release_held` (one-shot), `_ai_prompt_blocked`+block metadata. **(2) network_loop.cpp:1234-1257** is the bridge — after `tuple.on_packet()` returns, it translates session state to per-packet flags: block-first ordering (`_ai_prompt_blocked` checked before hold, clears hold at line 1245 to prevent double-flag packets), hold flag is sticky (re-asserted every packet at line 1250), release is one-shot (cleared at line 1256). **(3) visible_tls_session.cpp:540-727** is the per-packet VTS dispatcher — 5-branch cascade: (A) `_apf_release_held`+buffer→flush to server via `write_visible_data(&_sproxy)` cpp:547-566, (B) new hold activate cpp:570-579, (C) continued hold buffer with **64KB safety valve** cpp:583-611, (D) `_block_response_string` emit cpp:612-727 with discard held buffer + **direct SSL_write to client via &_cproxy (Phase3-B15 bypasses visible pipe to prevent H2 frame interleaving)** + delayed-ES (h2_end_stream=2 only, 10ms usleep + raw 9-byte DATA frame with END_STREAM=1 encoded manually) + RST_STREAM conditional + teardown, (E) default forward. **RST_STREAM conditional (Phase3-B24, cpp:684)**: only sent if `!was_held`. If held → server never saw stream → RST_STREAM with unknown stream_id would be protocol error causing GOAWAY. If NOT held (h2_hold_request=0) → server generating response → need to cancel. This is why h2_mode=2 can pair with either h2_hold_request value. **Huggingface 12-step end-to-end trace captured** in §17.6: fetch POST → HEADERS hold → DATA hold → keyword detect → block_session → network_loop translates flags → VTS branch D discards held buffer (server NEVER sees sensitive body) → direct SSL_write to client → RST_STREAM SKIPPED (was_held) → no on_disconnected → H2 connection alive → ReadableStream 2-frame clean termination → chat bubble shows warning. **Every step code-audited across cycles 41-55 — no unknown boxes remain** in the HF Phase 6 wire path. **MAJOR FINDING — Test-log contamination expanded from 2 to 15**: cycle 52 flagged 2 `[APF_WARNING_TEST:...]` tags in ai_prompt_filter.cpp. Grep of visible_tls_session.cpp finds **13 MORE**: hold_flush / hold_flush_done / hold_flush_partial / hold_activate / hold_buffer(x2) / hold_overflow / hold_discard / vts_pre / vts_post / vts_keepalive / vts_rst_server / vts_no_rst. **Every block event emits 5-8 of these in production.** Phase 7 release-gate grep strategy "if any `[APF_WARNING_TEST:...]` log appears, a test log was left in" is **definitively broken** — it would flag every production block event as a release-blocker. Correctly-named sibling `[APF:delayed_ES]` at cpp:668 proves the right pattern exists, just wasn't applied consistently. **Cycle 52's spawned side task scope must expand** from 2 lines to 15 lines. Wrote **envelope_audit §17** (sections 17.1-17.9): 3-module architecture diagram, session flag lifetime table (sticky vs one-shot), network_loop bridge code walkthrough, VTS 5-branch dispatcher detail, RST_STREAM semantics explanation, HF 12-step end-to-end trace, FULL 15-tag contamination surface map (with file:line:tag:fires-on table), verification level count update (now **14**: a-n — adds "VTS-layer 3-module pipeline end-to-end"), cross-references to Phase3 build tags B13/B14/B15/B16/B24/B30. **Significance**: closes the "unseen half" of h2_mode=2 keep-alive semantics. The VTS-layer code was referenced as "downstream" in §14 but never code-read. Now the entire huggingface Phase 6 wire pipeline is mapped with line-level references across 3 modules — ai_prompt_filter + etap core + visible_tls. No further audit work is needed for HF Phase 6 migration confidence. Verification levels for HF Phase 6 migration now at **14** (a-n). Remaining cycle 56+ candidates: (ee) full [APF:] log tag inventory across apf_filter + visible_tls for observability map (complements §17.7 contamination), (ff) ai_prompt_keyword.cpp keyword-matching internals audit, (gg) cross-check baseline SQL Part 3 envelope templates against cycle 48 placeholder rules. |
| 22:28 | **Cycle 54 — prepare_response_type / is_prepare_api selection gate audited; HF confirmed prepare-irrelevant; chatgpt-only in baseline.** Empty poll #28 (#454 still absent at +290min, **4.9x #453 precedent**, HF flat ~132min). Pivoted to cycle 53 plan candidate (aa): audit prepare_response_type vs response_type distinction at cpp:900 / cpp:1625-1628 / cpp:525 / cpp:705. **Core finding — is_prepare_api HARDCODED to "/prepare" suffix**: both H1.1 (cpp:525-526) and H2 (cpp:705-706) detection sites are `path.compare(path.size()-8, 8, "/prepare") == 0`. Byte-for-byte identical. No DB column, no config flag, no per-service override. Cycle 17 v0 Phase 6 design had noted this constraint when `/chat/api/send` couldn't use the mechanism; now fully traced and documented. **Lookup truth table** (cpp:1625-1628): `lookup_key = (is_prepare_api && !prepare_response_type.empty()) ? prepare_response_type : response_type`. 4 cases analyzed; only case 4 (both conditions true) is "active". Empty prepare_response_type (default) means /prepare path falls back to main envelope — detection is a no-op. Any future service wanting prepare semantics must ALSO have its pre-validation endpoint structured to end in `/prepare`. **Baseline SQL population** via grep of apf_db_driven_migration.sql: **EXACTLY ONE** service populated — chatgpt (line 39): `prepare_response_type='chatgpt_prepare', response_type='chatgpt_sse'`. The chatgpt_prepare envelope (SQL:72-85) is a **plain JSON error** `{"status":"error","error_code":"content_policy_violation","error":"{{MESSAGE_RAW}}"}` — incompatible with SSE delta framing that chatgpt_sse uses. This is the only legitimate use case: chatgpt's synchronous /prepare pre-validation needs structured JSON, its main streaming POST needs SSE delta. **Huggingface implication**: HF chat-ui SvelteKit routes (POST /conversation/{id}, /stop-generating, /settings, /api/*) have NO /prepare suffix → sd->is_prepare_api always false → lookup_key always = sd->response_type → **HF Phase 6 must NOT populate prepare_response_type**. Added explicit pre-apply instruction in §16.7. **H2 ordering hazard flagged**: on_http2_request re-detects is_prepare_api at cpp:704-706 AFTER the hold-set block at cpp:691-702. Verified hold conditional (cpp:691) only checks is_post/h2_hold_request/check_completed — prepare-agnostic, so no active bug. But fragile: any future edit adding is_prepare_api to the hold condition would introduce stale-read bug on H2. Flagged as low-priority defensive cleanup. **Reload validation** (cpp:115-120): warns if prepare_response_type set but no matching envelope row exists — service NOT disabled, degrades to plain http_response text at block time. Wrote **envelope_audit §16** (sections 16.1-16.11): why-matters, hardcoded detection two call sites, DB cache at detect_and_mark_ai_service, lookup gate with 4-case truth table, current baseline (chatgpt only), chatgpt_prepare vs chatgpt_sse distinctness rationale, HF relevance with explicit Phase 6 pre-apply instruction, H2 ordering hazard analysis, reload validation, verification level count (now **13**: a-m), cross-references to cycles 17/48/51/52. **Significance**: cycle 17 noted the constraint in passing; cycle 48 mentioned prepare distinction but didn't trace it; this is the first full end-to-end audit. Verification levels for HF Phase 6 migration now at **13** (a-m): DB schema / runtime envelope map / byte-level baseline / CLI completeness / SQL idempotency / http_response semantics / placeholder surface / H2 frame conversion / Content-Length rewrite branches / service detection+h2_mode ternary / request hold-release mechanism / B26 de-chunker defensive / **prepare_response_type selection gate**. Remaining cycle 55+ candidates: (bb) grep [APF:] log tags for full observability map, (cc) HPACK encoder static-table usage detail, (dd) visible_tls_session.cpp _apf_hold_for_inspection handler audit (where held buffer is actually discarded outside ai_prompt_filter module). |
| 22:23 | **Cycle 53 — B26 de-chunker + B27 header-body separator audited; B26 confirmed no-op for HF normal flow, belt-and-suspenders defensive only.** Empty poll #27 (#454 still absent at +282min, **4.8x #453 precedent**, HF flat ~127min). Pivoted to cycle 52 plan candidate (z): audit the B26 de-chunk companion path referenced in cycle 50 §12 TL;DR at `ai_prompt_filter.cpp:1412-1442`. Read full `convert_to_http2_response` prologue (1373-1442) + `generate_block_response` (1602-1677) for end-to-end propagation context. **B27 CRLF/LF separator logic** (cpp:1376-1404): searches BOTH `\r\n\r\n` AND `\n\n`, picks the EARLIER occurrence as the real header-body split. Subtle but critical — SSE bodies that went through `recalculate_content_length` branch A (cycle 50 finding) are wrapped in chunked encoding, so the body itself contains `\r\n\r\n` between chunks. Naive "first `\r\n\r\n`" would misread chunk terminators as header end → HPACK corruption → `ERR_HTTP2_COMPRESSION_ERROR`. **B26 de-chunker** (cpp:1412-1442) has three guards: (1) `first_crlf < 16` (chunk size hex prefix ≤ 15 chars), (2) all bytes in `[0, first_crlf)` must be `[0-9a-fA-F]`, (3) body must END with exact 7-byte `\r\n0\r\n\r\n`. All three must pass; if so, body is replaced with `body[first_crlf+2 : size-7]`. **Fast-fail analysis for HF normal flow**: body starts with SSE `data: ...` → no CRLF (SSE uses LF) OR 't' at index 2 fails hex check. Body starts with `{"type":...` → no CRLF. B26 is a guaranteed no-op on HF's non-chunked SSE or non-SSE JSON bodies. **When does B26 actually fire?** Only when `render_envelope_template` ran with is_h2=false (branch A applies chunking) BUT the chunked body then reached the H2 path. In the current code at cpp:1635 (`render_envelope_template(..., sd->is_http2)`) + cpp:1647 early return for non-H2, these steps **cannot co-occur** on the production path. B26 is pure belt-and-suspenders defensive coding for future refactors / test fixtures / alternate call sites that might wire flags incorrectly. The guard cost is negligible (3 fast-fails). **h2_end_stream ternary finding (cpp:1664-1668 Phase3-B30)**: 0=no ES, 1=immediate (2-frame: DATA body + DATA empty ES), 2=delayed (VTS layer 10ms later). `use_end_stream = (sd->h2_end_stream == 1)` — so 0 and 2 behave identically at the generate_block_response layer, differentiation happens at VTS downstream. First cycle encountering h2_end_stream as a ternary, parallel to cycle 51's h2_mode ternary discovery. HF's h2_end_stream value NOT captured in cycle 51 DB snapshot — TODO for next DB window. **Side finding — cpp:1655-1656 comment inaccurate**: comment says `replace(pos, 22, "Connection: close")` is "length-preserving" but "Connection: close" is 17 bytes vs 22, so the replacement shortens by 5 bytes. Not a bug (Connection is a header, doesn't affect Content-Length) but comment misleading. Low-priority docs fix, skipping side-task spawn. Wrote **envelope_audit §15** (sections 15.1-15.9): code path summary, B27 separator logic walkthrough, B26 de-chunker guards, fast-fail analysis for 3 body shapes, "when B26 actually fires" defensive-coding discussion, h2_end_stream ternary + HF unknown-value flag, Connection header rewrite side note, verification level count update (now **12**: a-l), cross-references. **Significance**: closes the "B26 follow-up" item from cycle 50 TL;DR. Verification levels for HF Phase 6 migration now at **12** (a-l): DB schema / runtime envelope map / byte-level baseline / CLI completeness / SQL idempotency / http_response semantics / placeholder surface / H2 frame conversion / Content-Length rewrite branches / service detection+h2_mode ternary / request hold-release mechanism / **B26 de-chunker defensive path**. Remaining cycle 54+ candidates: (aa) prepare_response_type vs response_type distinction (cpp:900, cpp:1625-1628 lookup_key selection — chatgpt_sse baseline uses chatgpt_prepare), (bb) VTS-layer block_session_h2 emission outside ai_prompt_filter module, (cc) grep [APF:h2_*] log tags to map full H2 observability surface. |
| 22:18 | **Cycle 52 — request hold/release mechanism fully audited (5 call sites, body_complete trap, Phase3-B19 guard, defensive path, HF flow trace) + test-log contamination FOUND in production hot path.** Empty poll #26 (#454 at +277min, **4.7x #453 precedent**). Pivoted to cycle 51 plan candidate (y): audit `_apf_hold_for_inspection` / `_apf_release_held` mechanism since HF has h2_hold_request=1. Read `ai_prompt_filter.cpp:525-551` (H1.1 hold-set), 596-630 (H1.1 hold-release), 631-702 (H2 hold-set), 728-837 (H2 hold-release + body_complete), 920-941 (SKIP_HOLD_RELEASE defensive path). **Complete hold mechanism picture**: (1) **Hold set** at HEADERS for POST+h2_hold_request=1+!check_completed, two call sites. (2) **Hold released** in DATA handler when `!blocked && body_complete`, two call sites per protocol. (3) **body_complete trap** at cpp:814-815: `http2_parser::set_end_of_body` runs AFTER the callback, so `_end_of_body` is ALWAYS 0 for H2 — `_download_length >= _content_length` is the ONLY reliable H2 check. (4) **Phase3-B19 guard**: check_completed=1 sessions skip hold entirely on follow-up POSTs — otherwise held buffer blocks PING ACK + WINDOW_UPDATE → ERR_CONNECTION_CLOSED → keep-alive service breaks. (5) **SKIP_HOLD_RELEASE defensive path** at cpp:935-939 catches race-window stuck holds. (6) **GET/HEAD never held** (hold would never release without body). (7) **WebSocket upgrade** bypasses hold at cpp:842-865 (no keyword check yet — future work). (8) Phase3 build tags B13/B16/B19/B25 capture the history. **Huggingface flow trace end-to-end**: POST /chat/... HEADERS → detect+hold → DATA with keyword → block_session_h2 sets blocked+check_completed → VTS discards held buffer + emits block response via convert_to_http2_response → h2_goaway=0 keeps connection open → next POST hits B19 guard (skip hold) → forwarded cleanly. **Every step code-audited across cycles 41-52 — no unknown boxes remain**. **PRODUCTION HOT PATH TEST-LOG CONTAMINATION FOUND**: cpp:826 `[APF_WARNING_TEST:hold_release]` and cpp:834 `[APF_WARNING_TEST:hold_continue]` use the Test Log Protocol's RESERVED prefix in production hot path, which the HTTP/1.1 sibling at cpp:626 correctly avoids (it uses `[APF:hold_release_h1]`). This defeats the Phase 7 release-gate grep strategy "if any `[APF_WARNING_TEST:...]` log appears, a test log was left in" — the 2 hold logs fire on every clean request in normal production. **Spawned side task** to rename both tags to `[APF:hold_release]` / `[APF:hold_continue]`. Added 2 new pre-apply verification items (JSON body+Content-Length header, no WebSocket upgrade). Wrote **envelope_audit §14** (10 sections): why-hold rationale with race-condition explanation, H1.1+H2 hold-set/release call-site table, body_complete 3-way OR with H2 trap note, Phase3-B19 check_completed guard, SKIP_HOLD_RELEASE defense, test-log contamination flag with fix recommendation, full 7-step HF flow trace, pre-apply verification checklist (now 7 items across §13+§14), build history tags, cross-references. **Significance**: this closes the cycle 49-51 thread on huggingface Phase 6 confidence — the VTS-layer hold mechanism (the "unseen half" of the h2_mode=2 keep-alive semantics) is now documented with the same rigor as the frame-assembly half. Verification levels at **11** (a-k): DB schema / runtime envelope map / byte-level baseline / CLI completeness / SQL idempotency / http_response semantics / placeholder surface / H2 frame conversion / Content-Length rewrite branches / service detection+h2_mode ternary / **request hold-release mechanism**. Cycle 53+ candidates: (z) B26 de-chunk companion path (cpp:1412-1413), (aa) prepare_response_type distinction (chatgpt_sse uses chatgpt_prepare in baseline), (bb) VTS-layer block response emission code audit. |
| 22:13 | **Cycle 51 — `detect_and_mark_ai_service` + `detect_service` + `domain_matcher` + `path_matcher` audited; MAJOR CORRECTION: huggingface is `h2_mode=2` not 1.** Empty poll #25 (#454 at +272min, **4.6x #453 precedent**). Pivoted to cycle 50 plan candidate (x): audit service classification path. Read `ai_prompt_filter.cpp:870-918` (detect_and_mark_ai_service) + cpp:199-298 (ai_services_list::detect_service) + cpp:72-193 (domain_matcher + path_matcher). Live DB query captured huggingface ai_prompt_services row: `domain_patterns='huggingface.co,*.huggingface.co', path_patterns='/chat', h2_mode=2, h2_end_stream=1, h2_goaway=0, h2_hold_request=1`. **MAJOR CORRECTION**: cycles 49/50 wrote h2_mode=1 throughout. Per `ai_prompt_filter_db_config_loader.h:35` comment, **h2_mode is a TERNARY**: 0=HTTP/1.1, 1=H2 cascade shutdown, 2=H2 keep-alive. Runtime propagation at cpp:1058-1059: `_ai_prompt_block_is_http2 = is_http2 ? h2_mode : 0` — NOT a boolean. **Modes 1 and 2 produce identical convert_to_http2_response frame assembly** (cycles 49/50 findings still valid); the distinction is at the VTS layer downstream — mode 1 tears down connection (GOAWAY/disconnect), mode 2 keeps connection alive for HF chat-ui navigation reuse. Fixed huggingface_design.md item 5 inline (replaced h2_mode=1 with ternary explanation). **h2_mode distribution table captured** from baseline SQL: chatgpt/claude/gemini/m365_copilot=1 (cascade); perplexity/perfle/genspark/grok/github_copilot/gamma/notion/huggingface=2 (keep-alive). **Pattern**: h2_mode=2 virtually always pairs with h2_hold_request=1 (exception: notion). Makes sense — if connection survives the block, upstream forwarding MUST be held so request body doesn't leak. **Phase3-B30 caveat**: h2_end_stream=2 means "delayed END_STREAM — VTS가 지연 전송" (perplexity/perfle/gamma use this); huggingface has h2_end_stream=1 (normal), no caveat applies. **detect_service priority algorithm documented**: per-service loop, **first matching domain pattern wins** (`break` at cpp:244 — pattern order matters!), priority = (1000+len literal OR 500+len wildcard) for both domain+path, highest total_priority candidate wins. **domain_matcher grammar**: 4 pattern kinds documented — (1) `[*.]literal` root+sub, (2) `*.literal` sub-only (root EXPLICITLY EXCLUDED at line 99-101), (3) `literal.*` trailing wildcard for TLD variance, (4) `literal` exact fallback. Huggingface's `huggingface.co,*.huggingface.co` pair is equivalent to `[*.]huggingface.co` — stylistic variant only. **path_matcher grammar**: wildcard (regex conversion with `*`→`.*`, full match with `^$`) OR literal prefix (exact-or-prefix-with-separator check). **Path `/chat` test cases** verified: `/chat` ✓, `/chat/` ✓, `/chat/conversation/xxx/messages` ✓, `/chatting` ✗, `/api/chat` ✗ — correct behavior. **PRE-APPLY FLAG**: if HF chat-ui streaming endpoint is NOT under `/chat/` prefix (e.g. `/api/conversation/xxx`), Phase 6 migration must ALSO update `path_patterns`. Can't verify without #454. Wrote **envelope_audit §13** (sections 13.1-13.8): h2_mode ternary doc, distribution table (12 services), detect_service priority walkthrough, path /chat test matrix, 4-item pre-Phase6-apply verification checklist, correction log, cross-references. **Significance**: cycle 51 both captures authoritative routing grammar AND corrects the cycle 49/50 h2_mode=1 oversight. Verification levels for HF Phase 6 migration now at **10** (a-j): DB schema / runtime envelope map / byte-level baseline / CLI completeness / SQL idempotency / http_response semantics / placeholder surface / H2 frame conversion / Content-Length rewrite branches / **service detection + h2_mode ternary**. Remaining cycle 52+ candidates: (y) apf_hold_for_inspection mechanism (cpp:930-941 SKIP_HOLD_RELEASE hints at subtle race), (z) B26 de-chunk companion path (cpp:1412-1413), (aa) prepare_response_type distinction. |
| 22:08 | **Cycle 50 — `recalculate_content_length` THREE-branch audit corrects cycle 48 oversimplification; huggingface wire path now fully audited end-to-end.** Empty poll #24 (#454 at +267min, **4.5x #453 precedent**). Pivoted to cycle 49 plan candidate (w): read `ai_prompt_filter.cpp:1139-1247` (`recalculate_content_length` full body). **MAJOR FINDING — the cycle 48 §11.5 rule "Content-Length: 0 is ALWAYS rewritten" is WRONG for SSE envelopes**. Function has **THREE branches**, classified by Content-Type sniffing for `text/event-stream` + `is_h2` flag: **(A) SSE + HTTP/1.1** (Phase3-B25d, #370-373 diagnosis): removes Content-Length, adds Transfer-Encoding: chunked, wraps body in HTTP chunked encoding format `<hex>\r\n<body>\r\n0\r\n\r\n`, sets Connection: keep-alive — mimics real qwen3 wire pattern because Chrome's EventStream parser needs chunked encoding + keep-alive to emit events. **(B) SSE + HTTP/2** (Phase3-B29, B28 discovery): removes BOTH Content-Length AND Transfer-Encoding, returns body verbatim — H2 DATA frame + END_STREAM handles boundaries, Content-Length breaks browser streaming mode. **(C) Non-SSE** (the "classic" path): replaces Content-Length with actual `body.size()` or adds it. **Key insight**: SSE/non-SSE split is Content-Type sniff, NOT DB response_type — any envelope with `text/event-stream` header routes to branch A or B regardless of what column name it has. Huggingface routes through **branch B**: Content-Length:0 is STRIPPED (not rewritten), then `convert_to_http2_response` forbidden-header filter (cycle 49) would strip it AGAIN = **TRIPLE-SAFE redundancy** (envelope dummy / recalc B strip / H2 conversion strip). Wrote **envelope_audit §12** (sections 12.1-12.8): per-branch code walkthrough with line numbers, Content-Type classification table (HF/copilot/deepseek/m365 all branch B, v0_html/v0_303 branch C), history tag map (B22/B25d/B26/B28/B29), explicit correction to §11.5 item 3, huggingface-specific end-to-end wire path re-audit (envelope → recalculate_content_length B → convert_to_http2_response → HEADERS + DATA(body, END_STREAM=0) + DATA(empty, END_STREAM=1)), and cross-references to cycles 48/49. **Significance**: the cycle 48 placeholder doc had ONE oversimplification — now closed. Verification levels for HF Phase 6 migration now at **9** (a-i): DB schema cycle 39 / runtime envelope map cycle 41 / byte-level baseline cycle 42 / CLI completeness cycle 43 / SQL idempotency cycle 45 / http_response semantics cycle 47 / placeholder surface cycle 48 / H2 frame conversion cycle 49 / **Content-Length rewrite branches cycle 50**. Also discovered **B26 de-chunk companion path** at cpp:1412-1413 — candidate for future audit. Remaining cycle 51+ candidates: (x) detect_and_mark service classification, (y) domain_matcher::match huggingface.co alias handling, (z) B26 de-chunk path. |
| 22:03 | **Cycle 49 — `convert_to_http2_response` audited; Build #20 2-frame DATA strategy is the right fit for huggingface chat-ui's fetch+ReadableStream SSE parser.** Empty poll #23 (#454 at +262min, **4.4x #453 precedent**). Pivoted to cycle 48 plan candidate (u): read `ai_prompt_filter.cpp:1094-1226` (convert_to_http2_response full body) to audit how the rendered HTTP/1.1 envelope is converted to H2 frames for the huggingface profile `h2_mode=1, h2_end_stream=1, h2_goaway=0`. **FOUR findings captured**: (1) **Build #20 2-frame DATA strategy** (~lines 1183-1203): when `end_stream=true && !body.empty()`, function splits body delivery into `DATA(body, END_STREAM=0)` + `DATA(empty, END_STREAM=1)` rather than a single terminal frame. Rationale from code comment: single-frame terminal delivery makes Chrome Fetch `ReadableStream.read()` resolve to `{value, done:true}` in one tick, and SOME SSE parsers check `done` first and drop `value` (Copilot #032 failure mode, 0 renders). **HF chat-ui uses `fetch()` + `response.body.getReader()`** in SvelteKit (not EventSource), hits the SAME code path as Copilot — Build #20 is correct fit, no override needed. (2) **Forbidden headers stripped** at ~1140-1143 per RFC 7540 §8.1.2.2: `content-length`, `transfer-encoding`, `connection` removed from HPACK header block. Our envelope's `Content-Length: 0` convention is **doubly safe**: `recalculate_content_length` rewrites it at render end, then `convert_to_http2_response` drops the header entirely. (3) **GOAWAY gate** at ~1216: `if (send_goaway) { ... }` — HF has `h2_goaway=0`, no GOAWAY frame emitted, SSE client self-closes on `END_STREAM=1` — correct semantics for chat-ui's fetch reader. (4) **HPACK literal-without-indexing** (0x00 prefix) with single-byte length, silent truncation >127 bytes. HF envelope longest header `Content-Type: text/event-stream; charset=utf-8` ~46B, `Cache-Control: no-cache` ~23B. Safe by **2.7x margin**. Appended findings to `huggingface_design.md` §Code verification as item 5 (4 sub-points). **Bonus fix**: caught that the INLINE SQL at line 131 of huggingface_design.md still had `http_response='BLOCK'` — cycle 47 fix only touched the addendum file, not this inline version inside the design doc. Fixed with canonical 159B text + cycle 47 note. Commit pending. **Significance**: end-to-end code audit of the H2 conversion path for huggingface's profile is complete. Verification levels now: (a) DB schema cycle 39, (b) runtime envelope map cycle 41, (c) byte-level DB baseline cycle 42, (d) CLI surface completeness cycle 43, (e) SQL idempotency cycle 45, (f) http_response semantics cycle 47, (g) envelope placeholder surface cycle 48, **(h) H2 frame conversion path cycle 49**. Remaining candidates for cycle 50+: (w) `recalculate_content_length` audit, (x) detect_and_mark service classification path, (v) test-PC visual for openai_compat_sse ESCAPE2 anomaly. |
| 21:58 | **Cycle 48 — Complete envelope-template placeholder surface captured; huggingface addendum verified placeholder-clean; 2nd drift finding.** Empty poll #22 (#454 at +253min, now **4.2x #453 precedent**). L4 show_stats delta vs cycle 46 baseline: `Total 11528→11530 (+2), m365_copilot 167→169 (+2 entire delta), HF=86 flat, claude 6957 flat, Blocked 32 flat, Total Checks 7588 flat`. HF flat ~98 min continuous. Pivoted to candidate (s): read `ai_prompt_filter.cpp:974-1049` (`render_envelope_template` full body) + cpp:910-930 (json_escape/json_escape2) + header:421-441 for authoritative placeholder documentation. **SIX placeholders exactly**: `{{MESSAGE}}` (json_escape once, direct JSON-string embed), `{{MESSAGE_RAW}}` (no escape at all), `{{ESCAPE2:MESSAGE}}` (json_escape twice, for JSON-in-JSON contexts), `{{TIMESTAMP}}` (ISO-8601 UTC lazy+cached), `{{BODY_INNER_LENGTH}}` (2-pass sentinel — byte count from marker to EOS, Gemini-specific), `{{UUID:<name>}}` (RFC 4122 v4 cached per-name per-render). **Unknown placeholders pass through verbatim** — typos fail silently with literal `{{KEY}}` leaking to user's chat bubble. **Content-Length: 0 is ALWAYS rewritten** by `recalculate_content_length` at render end (cpp:1048) — convention is write 0 and let C++ fix it. **{{MESSAGE}} vs {{ESCAPE2:MESSAGE}} rule**: receiver parse-layer depth. 1 layer (direct JSON embed) → MESSAGE; 2 layers (stringified JSON inside another JSON string, Gemini wrb.fr pattern) → ESCAPE2. Source baseline `apf_db_driven_migration.sql` uses ESCAPE2 **ONLY for gemini**. **ANOMALY flagged**: live DB `openai_compat_sse` envelope (cycle 42 decode) uses `{{ESCAPE2:MESSAGE}}` inside `data: {"choices":[{"delta":{"content":"{{ESCAPE2:MESSAGE}}"}...}]}` — a SINGLE-parse context. Per the rule, this would render real newlines as literal `\n\n` in the user's chat bubble after JSON.parse. 3 hypotheses documented (cosmetic bug accepted / client post-process / historical mistake frozen), test-PC visual needed to resolve. **DRIFT FINDING #2**: grep `openai_compat_sse` in `functions/ai_prompt_filter/sql/apf_db_driven_migration.sql` → **ZERO matches**. Live DB has 5 rows at priority=50 (chatglm/huggingface/kimi/qianwen/wrtn). Another set added via ad-hoc SSH SQL not under source control, companion to cycle 44 m365_copilot drift. Adds to cycle 45 §9 drift list. **huggingface addendum PART 1A re-audited**: uses only `{{MESSAGE}}` in single-parse contexts (`"token":"{{MESSAGE}}"`, `"text":"{{MESSAGE}}"`), Content-Length:0 auto-rewrite safe, `<CONTENT_TYPE>`/`<EVENT_SEP>` are source-level TBD markers NOT render placeholders (distinct `<...>` syntax avoids confusion). **No third latent bug**. Wrote **envelope_audit §11** (sections 11.1-11.6): complete placeholder table, parse-layer usage rule, anomaly hypotheses, drift finding, addendum verification, 6-item pre-INSERT verification checklist for future envelopes (grep non-matching `{{...}}`, parse-layer check, Content-Length convention, BODY_INNER_LENGTH semantics, UUID naming, `<...>` vs `{{...}}` separation). Committed `48042a5`, pushed. **Significance**: the placeholder surface was never authoritatively documented in one place before cycle 48 — it was scattered across cpp comments, header doxygen, and baseline SQL comments. Now it's one reference table with code line numbers. Phase 6 verification levels now: (a) DB schema cycle 39, (b) runtime envelope map cycle 41, (c) byte-level DB baseline cycle 42, (d) CLI surface completeness cycle 43, (e) SQL idempotency cycle 45, (f) http_response column semantics cycle 47, **(g) envelope placeholder surface cycle 48**. Dev-side productive work candidates for cycle 49+: (u) convert_to_http2_response + h2_* columns audit, (w) recalculate_content_length audit. |
| 21:10 | **Cycle 47 — `http_response='BLOCK'`/`'0'` latent bugs DISCOVERED and FIXED in draft Phase 6 SQL (2 files + canonical example source).** Empty poll #21 (#454 at +208min, no result — now **3.8x #453 precedent**, overall longest-wait record). Pivoted to cycle 46 plan candidate (q): read `ai_prompt_filter.cpp:1602-1677` (`generate_block_response`) to verify the envelope selection path beyond the `_envelopes` map. **MAJOR DISCOVERY (bug)**: the function calls `db_template = _config_loader->get_response_template(service_name)` and substitutes that value into envelope `{{MESSAGE}}` via `render_envelope_template(envelope, db_template, is_http2)`. Chasing the call chain: `get_response_template` (config_loader.h:265) → `_templates->find(service_name)` → loaded by `load_response_templates` (cpp:640-667) via `SELECT service_name, http_response, response_type, envelope_template FROM ai_prompt_response_templates WHERE enabled=1 ORDER BY priority DESC`. **Conclusion**: the `http_response` **column IS the block-message text** substituted into envelopes. **NOT a status code, NOT a placeholder.** The draft addendum and draft combined migration had been written with `http_response='BLOCK'` and `http_response=0` as "placeholders" that would have rendered literally in the user's chat bubble. **Bug severity check via live DB query** (`ssh ... mysql -h ogsvm -u root -p... etap`): (1) huggingface addendum PART 1A: existing row id=37 priority=50 with 159B canonical Korean+English text — new row with `'BLOCK'` at priority=50 creates a tiebreak race; InnoDB insertion order (id ASC) saves it today but this is undefined behavior per MySQL docs. (2) combined migration 1B.2b v0_api/v0_303_redirect: **v0_api has ZERO existing rows** — the new INSERT would be the only row → `{{MESSAGE}}` renders literal `'0'`. Currently LATENT because v0_303_redirect envelope has no `{{MESSAGE}}` placeholder (empty 303 redirect body), but any future edit adding an HTML body or headerized warning would immediately leak the `'0'`. (3) combined migration 1C.2 github_copilot/copilot_sse: **verified SAFE** — INSERT-SELECT pulls `t.http_response` from existing copilot_403 id=15 row. **Canonical convention captured** via live DB: priority=50 → 159B Korean+English long form (huggingface/chatglm/v0/copilot), priority=1 → 89B Korean short form (github_copilot/m365_copilot), deepseek has its own 176B variant at priority=90. **FIXES applied to 5 files**: (a) `phase6_huggingface_addendum_2026-04-15.sql` PART 1A — `'BLOCK'` → 159B text + 33-line BUG FIX comment block explaining why. (b) `phase6_combined_migration_2026-04-15.sql` section 1B.2b — `0` → 159B text + 13-line BUG FIX comment. (c) `services/envelope_audit_2026-04-15.md` — new **§10** (sections 10.1-10.6) with code walkthrough, 2-bug analysis, priority-tier table, verification checklist, and final grep pattern (`grep -E "http_response[^a-z]*(=|,)[^'\"]*['\"]?(BLOCK|0|NULL|TODO|TBD|PLACEHOLDER)"`). (d) `references/phase2-analysis-registration.md` — **fixed the canonical INSERT example** (was ALSO using `'BLOCK'`! — it was the ORIGINAL SOURCE of the bug pattern that got copied into the huggingface addendum) + priority-tier table + cross-ref to §10. (e) `references/operational-lessons.md` — Korean-language lesson entry matching existing format. **Alt (t) also executed** as belt-and-suspenders: grepped `functions/ai_prompt_filter/sql/*.sql` for `http_response=['"]?(BLOCK\|0\|NULL)['"]?` — **zero hits**. Baseline `apf_db_driven_migration.sql:93` chatgpt_sse INSERT uses the safe `INSERT-SELECT t.http_response FROM ... LIMIT 1` pattern. **No committed SQL files have the bug pattern** — it was contained to the 2 in-draft files I just fixed + the canonical template example. Committed `2af6e20`, pushed. **Significance**: without this cycle, the huggingface Phase 6 apply (scheduled for ~20 min after #454 arrives) might have shipped with a tiebreak-dependent bug that would have rendered `"BLOCK"` in the user's chat bubble if MySQL's PK ordering ever changed. The bigger win is the **canonical example fix** in phase2-analysis-registration.md — every future engineer running Phase 2 now sees the correct pattern first, not archaeology 40 cycles later. Phase 6 migration infrastructure is now hardened at **six levels**: (a) DB schema, (b) C++ runtime envelope map, (c) byte-level DB baseline, (d) CLI surface completeness, (e) SQL idempotency (cycle 45), **(f) `http_response` column semantics (cycle 47)**. |
| 20:55 | **Cycle 46 — DELETE-then-INSERT idempotency lesson propagated to 3 pipeline references.** Empty poll #20 (#454 at +186min, no result — 3.4x #453 precedent). **L4 show_stats delta vs cycle 45: EXACTLY FLAT** (11528/86/6957/167/32/7588). HF flat **66+ min continuous observation**. Per memory directive still not escalating. Pivoted to cycle 45 plan candidate (p): propagate the cycle 45 schema finding from `envelope_audit §9` into the pipeline reference docs so future Phase 5/6 designs pick up the correct pattern automatically without rediscovering the trap. **Three files updated**: (1) **`phase2-analysis-registration.md`** Step 3 — added ⚠️ subsection "INSERT idempotency — table-specific patterns" with side-by-side table comparison, 4-row list of historical duplicates in live DB, canonical safe DELETE-then-INSERT pattern, and explicit "DO NOT USE ODKU on ai_prompt_response_templates" warning. (2) **`apf-cli-commands.md`** — added ⚠️ "Table-specific INSERT idempotency" section **before** the Phase 6 apply command matrix, with quick-reference canonical envelope INSERT snippet and cross-references to both phase2 ref and envelope_audit §9. (3) **`operational-lessons.md`** — appended new Korean-language section "INSERT 멱등성 — 테이블별 패턴" matching the existing DB pattern verification procedure style, with canonical pattern snippet and 3-doc cross-reference. Committed `0f55ca3`, pushed. **Significance**: the cycle 45 finding was a **one-place discovery** — now it's a **four-place propagation** so it can't get lost. A new engineer loading the Phase 2 skill to register a new service will encounter the correct pattern in the first reference doc they read, not discover it by archaeology 40 cycles later like this session did. Combined with cycles 41-45, the Phase 6 migration infrastructure is now hardened at both the SQL and documentation layers. Remaining productive-work candidates getting thinner — cycle 47+ may need to probe non-HF services (m365/v0/deepseek) if #454 continues. |
| 20:50 | **Cycle 45 — CRITICAL schema finding: 3 Phase 6 INSERTs were non-idempotent; DELETE-then-INSERT fix applied.** Empty poll #19 (#454 at +181min, no result — 3.3x #453 precedent). **L4 show_stats delta vs cycle 44: EXACTLY FLAT** (11528/86/6957/167/32/7588). HF flat **61+ min continuous**. Per memory directive still not escalating. Pivoted to cycle 44 plan candidate (o): full live-DB drift audit via SSH MySQL SELECT on all rows of `ai_prompt_response_templates`. **54 rows captured as cycle 45 inventory checkpoint** (vs 11 in the Apr 8 baseline file — ~43 rows of drift). **MAJOR FINDING #1 (schema)**: `SHOW CREATE TABLE` reveals the table has **ONLY** `PRIMARY KEY (id)` (auto-increment surrogate) as its unique constraint; `idx_service_enabled` and `idx_priority` are BOTH non-unique. **No composite unique key** on (service_name, response_type). **MAJOR FINDING #2 (semantic bug)**: `ON DUPLICATE KEY UPDATE` is a **no-op** on this table — every INSERT gets a fresh auto-increment id so the duplicate-key check never triggers. Historical evidence: the live DB contains **3 identical `claude` rows** (all MD5 `022e27ac...`), **5 identical `openai_compat_sse` rows** (all MD5 `79553698...`), **2 identical `chatgpt_sse` rows** (all MD5 `aa64281b...`), and 7 identical `generic_sse` rows across character/clova/consensus/copilot/dola/phind/poe — all are re-run artifacts nobody noticed. **Runtime impact is ZERO for existing duplicates** because cycle 41's `_envelopes` map first-row-wins with identical content, but a future re-INSERT with **updated** template content would be SILENTLY IGNORED (priority tie → InnoDB insertion order keeps old row winning). **FIX**: DELETE-then-INSERT pattern applied to 3 Phase 6 INSERTs: (1) `phase6_huggingface_addendum` PART 1A (huggingface_sse), (2) `phase6_combined_migration` 1B.2b (v0_303_redirect), (3) `phase6_combined_migration` 1C.2 (copilot_sse). Each DELETE targets the exact (service_name, response_type) tuple its paired INSERT creates. Wrapped inside existing BEGIN/COMMIT for atomicity. **NOT amended** (intentional): `ai_prompt_services` INSERTs (that table HAS `UNIQUE KEY uk_service_name` so ODKU works correctly there); UPDATE-based deepseek_sse and v0_html_block_page migrations (idempotent by construction); existing duplicate rows in DB (cleanup deferred — cycle 41 runtime-dedup makes them harmless). Also added `envelope_audit_2026-04-15.md §9` with (a) full 54-row inventory snapshot as durable checkpoint, (b) schema finding with SHOW CREATE TABLE quote, (c) cycle 45 idempotency fix strategy documentation, (d) **25-row backport candidate list** of services existing in live DB but NOT in `apf_db_driven_migration.sql` (deepseek/v0/qwen3/blackbox/baidu/cohere/duckduckgo/meta/mistral/you + 7 generic_sse services + orphan/alias rows). Committed `4d1479a`, pushed. **Significance**: the Phase 6 migration plan had a latent **idempotency failure mode** that would have been exposed if anyone tried to re-apply a corrected template via re-INSERT. The bug was dormant because cycle 39's ODKU clause LOOKED idempotent but was semantically dead code. Discovered only through the cycle-44 finding that prompted a deeper schema audit. Phase 6 combined migration is now verified at **five distinct levels**: (a) DB schema (cycle 39), (b) C++ runtime envelope map (cycle 41), (c) byte-level DB baseline (cycle 42), (d) CLI surface completeness (cycle 43), (e) true SQL idempotency (cycle 45). Also: the 25-row drift list creates scope for a future `regen_baseline_migration.sh` side task but that's explicitly OUT of the Phase 6 hold path. |
| 20:45 | **Cycle 44 — m365_copilot_sse CORS concern MOOT at runtime, but source-tree DRIFT detected.** Empty poll #18 (#454 at +176min, no result — 3.2x #453 precedent). **L4 show_stats delta vs cycle 43 (5 min): Total 11526→11528 (+2), claude 6955→6957 (+2 entire delta), all others FLAT including HF=86.** Tiny claude +2 burst — same pattern as cycles 38/40 (small non-HF bursts every 5-15 min, HF never moves). HF flat for **56+ min continuous observation**. Per memory directive not escalating. Pivoted to cycle 43 plan candidate (n): audit the m365_copilot_sse CORS anomaly flagged in cycle 34 (`Access-Control-Allow-Origin: *` + `Allow-Credentials: true` is invalid CORS). Direct DB query via SSH: `SELECT HEX(envelope_template), MD5, LENGTH, priority` on `response_type='m365_copilot_sse'`. **FINDING #1**: cycle 34 concern is **MOOT at runtime** — live envelope uses `access-control-allow-origin: https://copilot.microsoft.com` (specific origin, VALID CORS), NOT `*`. Size 647 bytes, MD5 `02deeb5f4e81b6c718c4ee8ce8ffc325`, priority=1, enabled=1. Someone applied an ad-hoc SSH SQL UPDATE between Apr 8 and Apr 15 correcting the issue. **FINDING #2** (more interesting): this reveals a **source-tree DRIFT** case — `functions/ai_prompt_filter/sql/apf_db_driven_migration.sql:173-189` still has the invalid `*` variant. If disaster recovery re-runs the baseline migration, the valid CORS header regresses to invalid. Cycle 34 concern is thus non-moot at the "source integrity" level even though it's moot at runtime. **FINDING #3**: `Content-Length` placeholder evolved — baseline file uses `Content-Length: 0\r\n` (rewritten by `recalculate_content_length` in cpp), live DB uses `Content-Length: {{BODY_INNER_LENGTH}}\r\n` (rewritten at template render time). Two valid production patterns now coexist. Same placeholder pattern confirmed in `openai_compat_sse` (cycle 42) and `m365_copilot_sse` (cycle 44). Updated `envelope_audit_2026-04-15.md` with new **§8** documenting all 3 findings, the full decoded live envelope, and elevation of §6 item 2 (backport live DB → baseline file) from "optional" to "recommended". Cycle 42 + 44 now provide **two durable MD5 checkpoints** for future integrity audits: `7955369a54e3f47da70315d03aa28598` (openai_compat_sse, 342B) + `02deeb5f4e81b6c718c4ee8ce8ffc325` (m365_copilot_sse, 647B). Committed `c2402de`, pushed. **Significance**: a cycle-34 paper concern turned into a cycle-44 **process vulnerability finding** — the source tree is silently stale vs production for at least one service (probably more). This suggests a systematic drift-audit pass before any source-tree-based DB rebuild. Next cycle candidate (o) would expand the baseline capture to remaining services (deepseek/v0/blackbox/qwen3/baidu/etc). |
| 20:40 | **Cycle 43 — do_command confirmed as SOLE RPC entry; 9-command surface is EXHAUSTIVE.** Empty poll #17 (#454 at +171min, no result — 3.1x #453 precedent). **L4 show_stats delta vs cycle 42 (5 min): everything EXACTLY FLAT** (11526/86/6955/167/32/7586, log size=0). HF flat for **51+ min continuous observation**. Per memory directive still not escalating. Pivoted to cycle 42 plan candidate (l): grep `ai_prompt_filter.cpp` + header for any additional CLI dispatch paths beyond the 9 do_command commands mapped at cycle 35. Patterns searched: `RegisterCommand|register_command|add_command|bind_command|command_handler|handle_command|RPC|rpc_|exec_command` — **ZERO matches** inside `functions/ai_prompt_filter`. Header read confirms `ai_prompt_filter.h:270` declares `do_command` as `override` of the base `etap::rpc_connection` contract — this is the sole entry point. Cpp :256-451 dispatcher's final `else` branch at line 445 emits the authoritative error message `"Unknown command. Available: reload_keywords, reload_services, reload_templates, validate_template, show_stats, enable, disable, show_config, test_keyword\n"` — exactly the 9 commands documented. **Conclusion**: no hidden commands, no alternative RPC paths, no future-proofed stub handlers. The 9-command surface is **exhaustive**. Updated `references/apf-cli-commands.md` with: (1) completeness verification note citing cycle 43 grep + header + error-message triangulation, (2) canonical binary path `/home/solution/bin/etapcomm` (cycle 42 `find` confirmed NOT at `/home/solution/etap/bin/etapcomm` which earlier notes assumed), (3) dot-separator syntax requirement (`ai_prompt_filter.show_stats` works, space-separator returns `FAILED : Invalid command format` — confirmed via cycle 42-43 experiments), (4) 4 binary copy locations discovered via `find` with their roles. Committed `016870c`, pushed. **Significance**: closes the "could there be other CLI paths to de-risk Phase 6" question raised in cycle candidates lists across cycles 35/40/41/42. Phase 6 apply workflow is now **fully specified** — no additional tool discovery expected from further cpp reading. Combined with cycle 41 `_envelopes` map verification and cycle 42 canonical MD5 baseline, the huggingface migration plan is verified at **four distinct levels**: (a) DB schema (cycle 39 SQL), (b) C++ runtime envelope map (cycle 41), (c) byte-level DB baseline (cycle 42), (d) CLI surface completeness (cycle 43). |
| 20:35 | **Cycle 42 — Canonical openai_compat_sse baseline CAPTURED via direct DB query.** Empty poll #16 (#454 at +166min, no result — 3.0x #453 precedent). **L4 show_stats delta vs cycle 41 (5 min): everything EXACTLY FLAT** (Total 11526, HF 86, claude 6955, m365 167, Blocked 32, Total Checks 7586, log file size=0). HF flat for **46+ min continuous observation**. Per memory directive still not escalating. Pivoted to cycle 41 plan candidate (k): audit existing openai_compat_sse envelope for drift vs cycle 21 L2 snapshot via direct SSH MySQL query on 218.232.120.58. **MAJOR CANONICAL CAPTURE**: `SELECT service_name, response_type, LENGTH, MD5, priority, enabled FROM etap.ai_prompt_response_templates WHERE response_type='openai_compat_sse'` returned **5 rows (chatglm/huggingface/kimi/qianwen/wrtn), ALL IDENTICAL: 342B, MD5=`7955369a54e3f47da70315d03aa28598`, priority=50, enabled=1**. Fetched hex dump of chatglm row and decoded: actual envelope body **differs from cycle 21 L2 approximation** — real row has **1 data event** (not 2), merging `delta.content` + `finish_reason="stop"` into a single event. Also includes `Access-Control-Allow-Origin: *` + `Connection: keep-alive` + `Content-Length: {{BODY_INNER_LENGTH}}` + `{{ESCAPE2:MESSAGE}}` + `{{UUID:chatcmpl}}`. The 342B byte count matches exactly — only the per-event breakdown was approximated in cycle 21. Updated `phase6_huggingface_addendum_2026-04-15.sql`: (1) PART 0b expected block now cites the **exact MD5 value** and the decoded envelope body, (2) PART 2d expected block clarifies that **5 rows remain** post-migration (the `('huggingface','openai_compat_sse')` envelope ROW is orphaned from routing but physically untouched — only `ai_prompt_services.response_type` moves via PART 1B). Updated `huggingface_frontend.md` §3.2 with ground-truth snapshot replacing cycle 21 approximation, documenting the event-count discrepancy and recording the canonical baseline MD5. Committed `ef99fa2`, pushed. **Significance**: the PART 0 pre-check MD5 value is now a **known constant** (not TBD) — the impl journal at Phase 6 apply time can compare the fresh pre-migration query output against `7955369a54e3f47da70315d03aa28598` without an additional baseline-capture round-trip. Also: cycle 21's "5 services × 342B rows" claim is **DB-level verified** (not just L2-inferred). The shared-row INSERT pattern has now been verified at **three levels**: (a) C++ runtime via cycle 41 `_envelopes` map read, (b) DB schema via cycle 42 query showing 5 distinct rows on composite key, (c) byte-level integrity via cycle 42 MD5 match across all 5. Post-#454 confidence remains maximal. |
| 20:30 | **Cycle 41 — Code read confirms huggingface INSERT pattern SOUND at runtime level.** Empty poll #15 (#454 at +161min, no result — 2.9x #453 precedent). L4 delta vs cycle 40 (4 min): everything FLAT. HF flat for **41+ min continuous observation**. Per memory directive not escalating. Pivoted to cycle 40 plan candidate (j): read `ai_prompt_filter_db_config_loader.cpp:630-708` to verify envelope map lookup key shape. **MAJOR FINDING** (lines 671-679): `if (_envelopes->find(response_type) == _envelopes->end()) { _envelopes->emplace(std::move(response_type), std::move(envelope)); }` — the runtime `_envelopes` map is keyed by **response_type ALONE**, NOT by `(service_name, response_type)` composite. With `ORDER BY priority DESC`, duplicate response_type rows are discarded at runtime with the highest-priority one winning. **3 implications for HF migration** (all good news): **(a)** `openai_compat_sse` envelope is truly runtime-shared — regardless of how many rows exist in the DB, APF has exactly ONE `_envelopes['openai_compat_sse']` entry that all 5 services hit via their `ai_prompt_services.response_type='openai_compat_sse'` mapping. **(b)** Our INSERT pattern is provably sound: inserting `('huggingface', 'huggingface_sse', <new>)` creates a distinct `_envelopes['huggingface_sse']` entry with ZERO collateral impact on the shared entry — chatglm/kimi/qianwen/wrtn still resolve to the old (unchanged) envelope. **(c)** PART 2d MD5 check is still the right test: it catches the corner case where a priority-winning row gets altered and the 4 sibling services silently pivot to a different envelope. Updated `huggingface_design.md` §Code verification item 3 with the code quote + 3 consequences. Updated `phase6_huggingface_addendum_2026-04-15.sql`: (1) PART 0b comment block cites the runtime code path, (2) PART 2d removed `service_name != 'huggingface'` filter (now checks ALL rows regardless of schema shape), (3) added new **PART 2e** to verify `ai_prompt_services.response_type` routing for all 5 services post-migration (expect chatglm/kimi/qianwen/wrtn → openai_compat_sse unchanged, huggingface → huggingface_sse new). Committed `e9fdbaf`, pushed. **Significance**: the huggingface Phase 6 plan is now verified **at the C++ runtime level**, not just at the DB schema level. The INSERT pattern is not just "safe" but provably decoupled from sibling services at the `_envelopes` map level. Confidence for post-#454 apply is maximal. |
| 20:25 | **Cycle 40 — #455 check-warning request template drafted (DRAFT); L4 delta shows 2nd transient burst on m365_copilot.** Empty poll #14 (#454 at +156min, no result — 2.8x #453 precedent). **L4 show_stats delta vs cycle 39 (14 min): Total 11520→11526 (+6), m365_copilot 161→167 (+6, the entire delta), all other services FLAT including huggingface=86.** Pattern now clearly established: **small transient bursts on random non-HF services every 14-ish min** (cycle 38 claude +7, cycle 40 m365 +6), **zero HF activity for 37+ minutes of continuous observation**. Also noticed `Current log file: size=0` — rotated/cleared from cycle 36's 25189. Pattern reinforces background-test-PC-tabs theory over scenario-driven probes (which would be HF-specific). Per memory directive still not concluding stuck. Pivoted to cycle 39 plan candidate (h): drafted **`services/huggingface_check_warning_455_template.json`** (65 lines, id=`455-DRAFT`). 5-phase scenario: **Phase A** trigger blocked SSN prompt, **Phase B** screenshot warning bubble visibility + input box state, **Phase C** Network POST validation + body first-1KB base64 decode + Content-Type + status code, **Phase D** Console errors + chat session reusability check, **Phase E** (optional) chatglm regression probe as easiest access of the 4 openai_compat_sse siblings — if HF warning visible but chatglm changes → accidental shared-row corruption detected. **14-field result schema** (login_state, request_url, response_status, response_content_type, response_body_first_1kb_base64, warning_visible_in_bubble, warning_text_exact, markdown_rendered, input_box_reusable, console_errors, chat_session_reusable, regression_chatglm, screenshots, verdict, notes). **3 preconditions guard dispatch**: (1) #454 processed + huggingface_design.md LOCKED, (2) addendum SQL 2 TBD tokens resolved, (3) migration applied + validate_template [VALID]. Committed `fbb642d`, pushed. **Significance**: post-#454 deploy pipeline is now prepped **end-to-end** — design doc (SKELETON → 15 min promote) → addendum SQL (DRAFT → 5 min token substitution) → check-warning request template (DRAFT → 2 min field fill). Total wall clock from #454 arrival to test-PC dispatch: ~20 minutes. |
| 20:20 | **Cycle 39 — phase6 huggingface addendum SQL drafted (DRAFT, 2 TBD tokens); L4 delta FULLY FLAT again.** Empty poll #13 (#454 at +151min, no result — 2.7x #453 precedent). **L4 show_stats delta vs cycle 38 (11 min gap): Total 11520→11520 FLAT, huggingface 86→86 FLAT, claude 6955→6955 FLAT, Blocked 32→32 FLAT, Total Checks 7586→7586 FLAT.** The claude +7 burst from cycle 38 did NOT continue — was a transient, not sustained background noise. Everything frozen for 11+ min now. Per memory directive still not concluding stuck. Pivoted to cycle 38 plan candidate (g): drafted **`services/phase6_huggingface_addendum_2026-04-15.sql`** (313 lines, **DRAFT** status) as a standalone addendum to the main combined migration. Mirrors full PART 0/1/2/3/4 structure with **2 huggingface-specific additions**: **(1) PART 0b** captures MD5 of all 5 openai_compat_sse rows as shared-row baseline; **(2) PART 2d** compares post-migration MD5 of 4 non-HF rows (chatglm/kimi/qianwen/wrtn) against baseline — any differ = immediate rollback. This is the huggingface-specific safeguard against accidentally breaking 4 sibling services by touching the shared row. **2 TBD tokens** clearly marked: `<CONTENT_TYPE>` (text/event-stream vs application/x-ndjson) and `<EVENT_SEP>` (\n vs \n\n) — resolved by #454. Includes explicit **merge instructions** for absorption into combined migration once #454 confirms tokens (step-by-step renumbering of existing 1D→1E and 4d→4e sections). Main combined migration file unchanged — remains immediately runnable for 3 services in its current form. Committed `b02a329`, pushed. **Significance**: post-#454 apply workflow is now prepared down to SQL byte level. Once #454 result lands, the full huggingface Phase 6 can be ready in ~20 min (15 min design doc promotion + 5 min SQL token substitution), and applied standalone or merged into the 4-service combined window. |
| 20:15 | **Cycle 38 — huggingface_design.md DESIGN_SKELETON drafted; L4 delta FIRST MOVEMENT but ZERO huggingface traffic.** Empty poll #12 (#454 at +146min, no result). **L4 show_stats delta vs cycle 36 snapshot (14 min gap): Total 11513→11520 (+7), huggingface 86→86 FLAT, claude 6948→6955 (+7 — entire delta), Blocked 32→32 FLAT, Total Checks 7573→7586 (+13).** Traffic DID resume after the 8-min flat stretch, but ALL 7 new requests are to `claude` — **zero huggingface traffic in the last 14 min**. Two interpretations remain open and memory directive says not to conclude: (a) scenario wrapping up post-HF-probe (DOM/CSP/screenshot/serialization — zero network I/O expected during those phases, so claude +7 would be unrelated background noise), (b) scenario stuck, claude +7 is unrelated background. Pivoted to cycle 37 plan candidate (d): drafted **`services/huggingface_design.md`** (330 lines, **DESIGN_SKELETON** status until #454 lands). Follows deepseek/v0/github_copilot design pattern adapted for 5 huggingface-specific constraints: **(1)** INSERT new row pattern (NOT UPDATE) — the `openai_compat_sse` row is SHARED by 5 services (chatglm/huggingface/kimi/qianwen/wrtn); UPDATE would break the other 4. INSERT `huggingface_sse` row + UPDATE `ai_prompt_services.response_type` for huggingface only. **(2)** No 500B ceiling — huggingface has `h2_end_stream=1` (unlike deepseek's `h2_end_stream=2`), envelope ~268B raw / ~388B with 60B warning. **(3)** Same-origin (`huggingface.co` → `huggingface.co/chat/...`) — no CORS headers in envelope (unlike github_copilot). **(4)** Type-tagged JSON-lines body with 4 events (`status:started` + `stream` + `finalAnswer` + `status:finalAnswer`) matching public `huggingface/chat-ui` Svelte parser. **(5)** Single `{{MESSAGE}}` placeholder (NOT `{{ESCAPE2:MESSAGE}}`) — single JSON nesting level per cycle 31 lesson + `envelope_audit_2026-04-15.md §1` findings. Document includes ready-to-paste **PART 1D + PART 4D** addition blocks for `phase6_combined_migration_2026-04-15.sql` once #454 confirms the 2 TBD tokens (separator: `\n` vs `\n\n`; Content-Type: `text/event-stream` vs `application/x-ndjson`). Estimated SKELETON → LOCKED promotion time: **~15 min** after #454 lands. Committed `ad3de00`, pushed. **Also fixed 4 JSON parse-blocking stray-quote sites in pipeline_state.json** from earlier cycle updates (regex + iterative scan). **Significance**: huggingface Phase 5 design now exists — Phase 6 DB window grows from 3 → **4 services** (deepseek+v0+github_copilot+huggingface). If the window applies all 4 atomically, DONE count jumps 6→10 (+67%) — **largest single-cycle DONE-count jump in pipeline history.** |
| 20:10 | **Cycle 37 — huggingface_frontend.md PRE-CAPTURE SKELETON drafted; L4 counter delta FLAT.** Empty poll #11 (#454 at +141min, no result — far beyond #453's 55min precedent but L4 proved scenario IS running in cycle 36). **L4 show_stats delta check vs cycle 36 snapshot (8 min gap): Total AI Requests 11513 UNCHANGED, huggingface 86 UNCHANGED, Blocked 32 UNCHANGED.** Counter freeze means scenario is no longer producing HTTP traffic (not HF-specific — ALL 19 services flat). Two interpretations: (a) scenario finished HF probe phase, now in DOM inspection / CSP capture / screenshot / result serialization (no network I/O during those phases — entirely consistent with a long-tail scenario wrap-up), OR (b) scenario got stuck after HF probe. **Per memory directive `feedback_autonomous_mode.md` defer 'stuck' conclusion, pivoted to productive work.** Per `project_test_pc_always_on.md` MEMORY, trust test PC is up. Executed candidate (c) from cycle 36 plan: drafted **`services/huggingface_frontend.md` PRE-CAPTURE SKELETON** (234 lines, 12 sections + TBD checklist) consolidating everything already knowable from (1) cycle 21 L2 openai_compat_sse extraction, (2) 2026-04-14 09:59 etap.log historical blocked trail (`service=huggingface keyword=\d{6}-\d{7} response_type=openai_compat_sse h2_mode=2 h2_end_stream=1 http2=1`), (3) cycle 36 show_stats (huggingface=86 lifetime), (4) public `github.com/huggingface/chat-ui` Svelte source. **Pre-hypothesis locked**: HF uses type-tagged JSON-lines `{type:'stream',token}/{type:'finalAnswer',text}` (NOT openai SSE), proposed `huggingface_sse` envelope ~260B raw with 4 events (status-started + stream + finalAnswer + status-finalAnswer). **Migration approach**: INSERT new `(huggingface, huggingface_sse)` row — do NOT modify shared openai_compat_sse row (still serves chatglm/kimi/qianwen/wrtn). Option A verdict STRONG (pre-hyp); Options B/C/E blocked by Svelte compile-time i18n + CSR navigation; Option D partially viable for initial-load banner. **70% of document is useable now** — when #454 lands only TBD placeholders at §2 (POST body), §3.1 (wire format), §4 (DOM), §5 (error UI), §6 (CSP) need filling. Committed `16d9ca5`, pushed. **Significance**: huggingface would join deepseek+v0+github_copilot in Option A ready bucket → Phase 6 DB window grows from 3 to **4 services** in single transaction. Suggest extending `phase6_combined_migration_2026-04-15.sql` PART 1D for huggingface once #454 confirms pre-hyp. |
| 20:01 | **Cycle 36 — show_stats/show_config sampled; MAJOR #454 diagnostic breakthrough.** Empty poll #10 (#454 at +134min). Executed cycle 35 plan candidate (e) — sampled the two uninspected CLI commands. **show_stats MUCH richer than expected**: per-service lifetime request counters for all 19 active services + block counts + keyword matcher stats + log status. **Critical diagnostic: huggingface=86, v0=745, claude=6948, wrtn=2108, gemini3=112, m365_copilot=161, grok=377, github_copilot=23, deepseek=2, plus 10 more.** This is a **4th monitoring layer (L4)** — cheaper than L3 AnyDesk, more granular than L2 etap.log grep. **show_config much smaller than expected**: only 6 global options (Chunk Accumulate Threshold, Block On Detection, File Logging settings). Does NOT dump services/templates — cannot substitute for MySQL queries. **MAJOR #454 DIAGNOSIS**: huggingface=86 is decisive evidence that #454 scenario IS running. Today's etap.log has ZERO huggingface entries because Block Rate = 0.28% and frontend-inspect probes the SUCCESS path (all pass-through = all invisible in [I]-level log). The 134+ min wait is legitimate scenario execution, NOT a hang. **L3 escalation no longer needed.** Updated `references/apf-cli-commands.md` with real samples + 21-service counter output + schema notes + counter-scope caveat (lifetime vs daily). Committed `6cde541`, pushed. **Pipeline impact**: Phase 6 apply workflow now has 4-layer monitoring (L1 git poll, L2 etap.log grep, L3 AnyDesk, L4 show_stats per-service). Before-after show_stats diff can verify block counters move by exact test-request count. |
| 19:54 | **Cycle 35 — mapped full do_command dispatcher (9 commands, 2 new Phase 6 tools).** Empty poll #9 (#454 at +127min, no result; L2 etap.log grew to 20224740 @ 19:43 — SetCertificate 2-hourly cron burst at 19:43 confirms process/logger alive AT ERROR level, but [I] level STILL silent since 18:14:56 = 100min = intermittent writer pattern). Per memory directive pivoted to productive work: candidate (b) from cycle 33 plan. Read `ai_prompt_filter.cpp` lines 256-451 — complete `do_command` dispatcher with **9 runtime CLI commands**. Two new Phase 6 de-risk tools beyond cycle 31's validate_template: (1) **`test_keyword <text>`** — runs keyword matcher against arbitrary text, returns [MATCH FOUND]/[NO MATCH] with category + position. **HUGE de-risk** — pre-verifies trigger text matches a keyword rule BEFORE pushing 10min check-warning cycle to test PC. Eliminates the "sent, waited 10min, got no-warning, then L2 grep to find APF never saw the keyword" failure mode. (2) **`enable`/`disable` pair** — runtime APF toggle for baseline captures. disable → submit prompt → observe "APF-free" response → enable → resubmit → diff = what APF actually substituted. Isolates "APF did nothing" from "APF substituted something frontend can't render". Other 6 commands: `reload_keywords`/`reload_services`/`reload_templates` (manual reload if auto-trigger fails), `show_stats` (output_monitoring dump), `show_config` (full _config dump), `validate_template` (already known). Wrote **`references/apf-cli-commands.md`** (296 lines, 9 sections + full Phase 6 apply workflow with 8-step command sequence). Committed `0b5179a`, pushed. **Significance**: Phase 6 combined migration apply workflow is now MAXIMALLY de-risked — pre-verify templates (validate_template) AND keyword matches (test_keyword) BEFORE any test PC interaction. 25-30min DB window estimate drops to ~15-20min because failures are caught upfront. `show_stats` + `show_config` may also substitute for L2 MySQL queries in future cycles (format sampling flagged as low-priority follow-up). |
| 19:46 | **Cycle 34 — envelope audit: no hidden bugs in baseline migration.** Empty poll #8 (#454 at +120min, no result; L2 state static from cycle 33 — file size + mtime both unchanged for 23+ min so nothing writing [I] level logs server-wide). Per memory directive pivoted to productive work: candidate (a) from cycle 33 plan. Read all 333 lines of `functions/ai_prompt_filter/sql/apf_db_driven_migration.sql` and audited every envelope template across **11 services** for (1) ESCAPE2 placeholder misuse, (2) missing CORS headers, (3) structural issues. **Verdict: NO hidden bugs.** Key findings: (1) **`gemini` is the ONE legitimate ESCAPE2 user** — wrb.fr envelope has doubly-nested JSON (outer array string member contains inner JSON), so ESCAPE2 is semantically REQUIRED. Confirms cycle 31 github_copilot fix was correct. (2) **m365_copilot_sse CORS anomaly**: `Access-Control-Allow-Origin: *` + `Allow-Credentials: true` is INVALID per CORS spec. Currently works (likely same-origin fallback). Flagged for next m365 touch — replace `*` with `https://m365.cloud.microsoft`. Not blocking. (3) **notion/perplexity/genspark have no CORS** — all same-origin, verified OK. (4) **Separator conventions**: genspark_sse/perplexity_sse use `\n\n` (not `\r\n\r\n`), grok_ndjson/notion_ndjson use `\n` only. Documented as a rule — copy separator from specific frontend's observed capture. (5) **Source-tree drift confirmed**: baseline migration file has only 11 services, but live DB has 18+ (deepseek/v0/blackbox/qwen3/openai_compat_sse sharers/baidu added via ad-hoc SSH SQL). Written to **services/envelope_audit_2026-04-15.md** (122 lines, 6 sections). Committed `d90b062`, pushed. **Significance**: `phase6_combined_migration_2026-04-15.sql` can be applied with high confidence — no pre-existing envelopes carry latent bugs. Also establishes a reference document for future Phase 5 designs to avoid the cycle 31 bug class. |
| 19:33 | **Cycle 33 — validate_template pre-check propagated to deepseek + v0 designs.** Empty poll #7 (#454 at +113min, no result; heartbeat 18:25 stale 66min; L2 [I] silent 76min though etap.log file mtime 19:16 proves writer alive at process level). Per memory directive pivot to productive work: candidate (e) from cycle 32 plan. Edited `deepseek_design.md` and `v0_design.md` to add matching `## Phase 6 pre-check: validate_template CLI` subsections — same pattern as cycle 31's github_copilot_design.md update, adapted per-service. Deepseek version highlights the **500B h2_end_stream=2 ceiling check** (rendered_size must be ≤ 500B). v0 version validates **BOTH templates** (v0_html_block_page UPDATE + v0_303_redirect new INSERT) and references `phase6_combined_migration_2026-04-15.sql` PART 4 v0 revert block (which reverts v0 UPDATE AND DELETEs v0_api row) for rollback. Both docs now reference `etapcomm ai_prompt_filter.validate_template` as the canonical Phase 6 de-risk tool. Fixed step numbering in deepseek test criteria (inserted step 3 "Validate template"). Committed `ec419cb`, pushed. **Significance**: all three services in phase6_db_blocked bucket now carry the SAME pre-check procedure. Before this cycle, only github_copilot mentioned validate_template despite cycle 31 discovering it as a universal Phase 6 tool. Combined with `phase6_combined_migration_2026-04-15.sql` (cycle 32), the Phase 6 handoff is now maximally de-risked and ready to execute in a single DB window. |
| 19:23 | **Cycle 32 — Combined Phase 6 migration SQL drafted + pushed.** Empty poll #6 (#454 at +102min, no result; L2 and heartbeat both silent per expected stage-3 pattern). Per memory directive pivot to productive work. **Drafted `shared-skills/genai-apf-pipeline/services/phase6_combined_migration_2026-04-15.sql`** (~400 lines) — ready-to-execute combined migration for **deepseek + v0 + github_copilot** in a single DB transaction. Structure: **PART 0** pre-check queries (captures baseline + rollback state from existing rows), **PART 1** BEGIN/COMMIT main transaction (deepseek UPDATE in-place, v0 UPDATE + v0_api INSERT-ON-DUP-KEY, github_copilot UPDATE service + INSERT new copilot_sse row), **PART 2** DB state post-check, **PART 3** runtime validation via `etapcomm ai_prompt_filter.validate_template` for all 4 templates, **PART 4** per-service rollback blocks. All SQL sourced from respective design docs (deepseek_design.md, v0_design.md f+h pair, github_copilot_design.md cycle 31 corrected). **Ready to copy-paste when DB access window at 218.232.120.58 opens.** Estimated runtime: ~30-60s transaction + ~5s etap reload + 4x validate_template + test PC check-warning per service = ~25-30 minutes total. Committed `b81fe5b`. **Significance**: three Phase 5-designed services can now go from "DB-blocked" to "verified working" in a single ~30min DB session, with cycle 31's validate_template pre-check catching template rendering bugs BEFORE flipping ai_prompt_services.response_type. |
| 19:14 | **Cycle 31 — CRITICAL corrections to github_copilot design + NEW Phase 6 pre-check tool discovered.** Empty poll #5 (#454 at +93min, no result). Per memory directive pivot to productive dev-side work. Read `functions/ai_prompt_filter/sql/apf_db_driven_migration.sql` (existing baseline migration file, Apr 8, 26KB) and `ai_prompt_filter.cpp` lines 300-384 + 1107-1137. **THREE CRITICAL corrections to github_copilot_design.md**: (1) **CORS was NEVER the root cause** — apf_db_driven_migration.sql lines 138-153 proves existing `copilot_403` envelope ALREADY includes `access-control-allow-credentials: true` + `access-control-allow-origin: https://github.com`. Cycle 30's "CORS as root cause" diagnosis was **WRONG**. REAL root cause: GitHub's React Copilot app treats any non-200 as error and renders static-i18n primer-react Banner **regardless of body contents** (already proven in Phase 4 §5 via empty-500 fetch-override test). HTTP 200 + SSE is the actual fix because it enters the success code path. (2) **Placeholder bug**: `{{ESCAPE2:MESSAGE}}` must be `{{MESSAGE}}` (single json_escape). json_escape2 is `json_escape(json_escape(x))` (ai_prompt_filter.cpp:1124-1127) — would doubly-escape user text showing backslashes in the warning. All working SSE templates use `{{MESSAGE}}` or `{{MESSAGE_RAW}}`, never ESCAPE2 in SSE body. (3) **NEW tool**: `etapcomm ai_prompt_filter.validate_template <response_type>` — loads envelope from DB, test-renders with `__VALIDATION_TEST__`, validates HTTP status line + separator + Content-Length, returns [VALID]/[INVALID] + first 2048B rendered output (ai_prompt_filter.cpp:319-384). **HUGE Phase 6 de-risk** — can verify all three envelopes (copilot_sse, deepseek_sse, v0 f+h) BEFORE switching ai_prompt_services.response_type. Also documented INSERT-SELECT-ON-DUPLICATE-KEY-UPDATE pattern from chatgpt_sse (apf_db_driven_migration.sql:91-111) as the correct INSERT shape — unique key appears to be (service_name, response_type) composite. Committed `d980167`, pushed. Without these corrections Phase 6 would deploy broken envelopes (doubly-escaped warning text). |
| 19:02 | **Cycle 30 — CORS preservation open question RESOLVED by C++ code read.** git pull empty (#454 huggingface still in flight at +80min from 17:41 push — but #453 also took ~55min, per `project_test_pc_always_on.md` MEMORY trust test PC is working). L2 SSH: [I] entries still silent since 18:14:56 (47min); [E] Bypass entries continue (collector.github.com at 18:11:43 = #453 telemetry, not huggingface). heartbeat unchanged at 18:25 (37min stale — consistent with cycle 20 finding heartbeat only updates at cycle start). **Productive dev-side work: investigated the CORS preservation question I raised in `github_copilot_design.md §2`.** Read `ai_prompt_filter.cpp` lines 1249-1328 (`render_envelope_template`) and 1602-1677 (`generate_block_response`). **Findings**: (1) `render_envelope_template` is a **pure template renderer** — only placeholder substitution (`{{MESSAGE}}`, `{{ESCAPE2:MESSAGE}}`, `{{UUID:name}}`, `{{TIMESTAMP}}`, `{{BODY_INNER_LENGTH}}`) + Content-Length recalc. (2) `generate_block_response` loads `envelope_template` from DB via `_config_loader->get_envelope_template(lookup_key)`, passes verbatim through `render_envelope_template`, optionally HTTP/2-frames via `convert_to_http2_response`. (3) **Grep for `Access-Control-Allow|CORS|cors|access_control` across `ai_prompt_filter.cpp` returned ZERO matches** — APF has no upstream-header preservation code at all. **Conclusion**: APF synthesizes the ENTIRE response from scratch; the envelope_template DB row IS the wire response (modulo placeholders). **Implications**: (a) **My Phase 5 envelope template is correct as written** — CORS headers embedded in §2 will render into the response exactly. **No C++ code change required for Phase 6.** (b) Root cause of existing copilot_403 failure diagnosed: old template almost certainly lacks CORS headers, browser rejects the cross-origin 403 before React sees it, React falls back to static-i18n Banner. (c) Phase 6 pre-check addition: query existing copilot_403 envelope_template to validate root cause. Wrote definitive finding into `github_copilot_design.md §2 "Critical CORS notice"` + §8 open questions #1 marked RESOLVED. **This is significant de-risking** — github_copilot Phase 6 is now pure DB-only migration matching deepseek+v0. All three services can apply in a single DB transaction matching the cycle 27 addendum template. Pipeline focus remains on **#454 huggingface** result arrival. |
| 18:30 | **Cycle 26 — #453 GITHUB COPILOT WINNING RESULT, Phase 4+5 dual delivery.** Result landed 18:25 (~55min after request push due to slow scenario start). Outstanding capture, 19 screenshots. **Verdict: FRONTEND_PROFILED — Option A SSE injection HIGHLY VIABLE.** Key findings: (1) **API host = `api.individual.githubcopilot.com`** (cross-origin from github.com), endpoint `/github/chat/threads/{threadId}/messages` POST, response **`text/event-stream`**. (2) **Simple 2-event-type schema** — SIMPLER than DeepSeek: `data: {type:'content',body:<text>}` + `data: {type:'complete',id,parentMessageID,model:'',turnId:'',createdAt,references:[],role:'assistant',intent:'conversation',copilotAnnotations:{...}}`. (3) **window.fetch override worked** (unlike Google's Gemini): GitHub's fetch-patch.ts uses live window.fetch reference at call time → captured 373B stream verbatim. (4) **Phase B error UI**: empty 500 → primer-react Banner *'I'm sorry but there was an error. Please try again.'* STATIC i18n confirmed. **Options B and C BLOCKED** with hard evidence. Legacy `.js-flash`/`.flash-error` exists in DOM shell but UNUSED by Copilot. (5) **CSP host-based**, less strict than Gemini, largely irrelevant to APF (HTTP transport layer). (6) **5-way verdict**: A ✅ HIGHLY VIABLE / B ❌ / C ❌ / D ⚠️ initial-load only / E ❌ CSR constraint. (7) **Critical CORS detail**: APF MUST preserve `Access-Control-Allow-Origin: https://github.com` + `Access-Control-Allow-Credentials: true` + `Expose-Headers: x-github-request-id` from upstream — open question whether copilot_403 currently strips these (would explain why 403 body never reached UI). Wrote **services/github_copilot_frontend.md** (Phase 4, supersedes 2026-04-02 v1) and **services/github_copilot_design.md** (Phase 5, FULL Phase 6 migration SQL: INSERT new copilot_sse template + UPDATE ai_prompt_services + 2x revision_cnt bumps + pre-check + post-check + rollback). Body sizing ~710B fits under h2_end_stream=1 ceiling. **github_copilot status: phase4_frontend_inspect → phase5_designed**, joins deepseek + v0 in **phase6_db_blocked bucket (now 3 services)**. Combined transaction estimate ~25-30min. Pipeline focus pivots to **#454 huggingface** (still in flight, no L2 huggingface activity yet — test PC running scenarios sequentially). Pre-locked huggingface schema-mismatch hypothesis ready. |
| 18:07 | **Cycle 22 — L2 confirms #453 IN FLIGHT, no escalation.** Empty poll #4 at git layer, but L2 SSH found github_copilot POSTs landed at 18:03:29 (stream=3 dl=2 OPTIONS preflight) and 18:03:30 (stream=7 dl=416 prompt body — matches scenario Phase A 'Write a hello world function in Python'). Both releases as clean (no blocked keywords), 104 held bytes flushed to upstream. APF correctly identifies host as github_copilot. **Test PC is ALIVE** — the ~33min delay from request push (17:30) to first POST (18:03) is slow scenario start, NOT failure. Per `project_test_pc_always_on.md` MEMORY directive validated again. **Cleared `monitoring.visual_needed=false`** — L2 evidence supersedes 30min L3 trigger. #454 huggingface still untouched (test PC running sequentially, #454 follows #453). Expected timeline: **#453 result 18:13-18:18, #454 result 18:25-18:35**. Cycle 23 wake at 18:14 should catch #453 result. Pre-locked hypotheses from cycle 21 ready to validate immediately. |
| 18:00 | **Cycle 21 — Envelope template extraction + pre-hypothesis lock.** Empty poll #3. #453 at +30min (L3 escalation threshold), #454 at +20min. test PC etap log silent since 17:38:48, heartbeat stuck at 16:29 last_processed_id=450. Per `project_test_pc_always_on.md` MEMORY directive: do NOT interpret as test PC down — pivot to dev-side productive work. **L2 SSH harvest of `ai_prompt_response_templates` envelope bodies**: chatgpt_sse=1247B, copilot_403=346B, deepseek_sse=358B, openai_compat_sse=342B (5 duplicate rows: chatglm/huggingface/kimi/qianwen/wrtn). Captured full openai_compat_sse template body verbatim — confirmed it uses **OpenAI standard `data: {choices:[{delta:{content:...}}]}` wire format**. Captured copilot_403 partial — HTTP 403 + `{message,documentation_url,status:403}` JSON matching GitHub API error format. **Confirmed huggingface 빈 채팅 root cause hypothesis**: HF Svelte chat-ui (open-source github.com/huggingface/chat-ui) uses **type-tagged streaming** `{type:'stream',token:{text}}`/`{type:'finalAnswer',text}` — NOT OpenAI choices[].delta.content. Schema mismatch → parser silently discards → empty assistant bubble. Network-layer block IS landing (proven by historical block trace 2026-04-14 09:59:53: keyword `\d{6}-\d{7}` ssn category, http1_size=466, response_type=openai_compat_sse, h2_mode=2 stream=31). **Fix path for huggingface (Phase 6 ready)**: INSERT new `huggingface_sse` envelope template + UPDATE `ai_prompt_services SET response_type='huggingface_sse'`. Existing openai_compat_sse stays for chatglm/kimi/qianwen/wrtn. **Confirmed github_copilot hypothesis**: copilot_403 = HTTP 403 + JSON. GitHub UI almost certainly status-code-driven, ignores body message field, renders own i18n error (DeepSeek static-i18n + Gemini3 silent-fail family). 4 fix paths ranked: (1) HTTP 200 SSE mimicking copilot success format, (2) custom copilot_chat_sse, (3) block page substitution at github.com/copilot doc, (4) PENDING_INFRA. Intel saved to `local_archive/cycle20_l2_intel_2026-04-15.md` (cycle 21 addendum). **`monitoring.visual_needed=true` set** since #453 hit 30min mark — next foreground session should consider AnyDesk visual diagnosis per cowork-remote/references/visual-diagnosis.md. When #454/#453 land, deliverables can be written immediately from pre-locked hypotheses. |
| 17:47 | **Cycle 20 — L2 intel harvest while awaiting #453/#454.** Both requests confirmed on origin (`784016b`, `3c8c79d`), JSON valid. No new results. Last test PC commit was 17:22 (#452 result) → 25min quiet. Per MEMORY directive, checked **dev-side first** (clean) and **do NOT interpret quiet as test PC down**. L2 SSH harvest via `sudo mysql etap` + etap.log grep produced high-value prep intel saved to `local_archive/cycle20_l2_intel_2026-04-15.md`: (1) **github_copilot API host = `api.individual.githubcopilot.com/github/chat/`** (not github.com — that's just the UI shell), envelope `copilot_403` h2_mode=2 end_stream=1 goaway=0. (2) **huggingface uses shared `openai_compat_sse` envelope** with path `/chat`, h2_mode=2. Historical block trace (2026-04-14 09:59:53) shows huggingface.co/chat/conversation/<uuid> blocked cleanly with 466B envelope + keyword `\d{6}-\d{7}` (ssn). Rendered envelope is ~429B — well under 500B ceiling. (3) **Hypothesis for huggingface "빈 채팅"**: envelope lands but HF's Svelte chat-ui can't parse `openai_compat_sse` body shape — similar to gemini3's schema-mismatch hypothesis. Will validate via #454 result. (4) **Curiosity**: gemini3 POST holds on streams 1/3/5 at 17:38:45 (16min after #452 completed) are background-tab auto-refresh traffic, not scenario execution. (5) **Heartbeat staleness is a non-signal** — heartbeat updates only at cycle start, not during stage 3. Git commit activity is the better liveness signal. |
| 17:40 | **Cycle 19 — #454 huggingface dispatched** (DEV_DRIVES parallel draft). #453 still in flight at +10min (within 10-15min envelope). Per DEV_DRIVES mode, used the free Rate Limit Gate slot to dispatch **#454 huggingface frontend-inspect** (priority 6, modeled on #451/#452/#453). Special interest: Hugging Face chat-ui is **open-source SvelteKit** project — reverse-engineering is easier than other services. Core questions: (a) is streaming SSE (→ deepseek-style Option A applies) or NDJSON/chunked (→ different approach)? (b) is "빈 채팅" a Gemini-like silent-fail or a UI characteristic unique to Hugging Face? jhee28 OAuth session confirmed via #441 precheck. Rate Limit Gate: 2/2 (both #453 and #454 active). Pre-prep for #453 result: searched worktree for `huggingface`/copilot_403 envelope to anticipate findings — no copilot_403 matches in worktree, reinforcing source-tree drift pattern. |
| 17:30 | **Cycle 18 cont. — Gemini3 #452 processed + #453 dispatched.** During the #451 commit push, **#452 gemini3 result also landed** (commit `a7f3c58`). Verdict: `FRONTEND_PROFILED_WITH_BLOCKERS — silent fail confirmed, strict CSP blocks DOM, wrb.fr envelope is only path`. Key findings: (1) Silent-fail reproduced via DevTools Offline — **5 `jserror` POSTs to `/_/BardChatUi/jserror`** catch the error internally but Angular template has NO user-visible binding → silent-fail is an intentional UX decision by Google. (2) **CSP strict-dynamic nonce** (`script-src 'nonce-<val>' 'strict-dynamic'`) blocks all DOM injection paths at browser level, BEFORE JS executes. (3) Batchexecute protocol captured: URL template, 5 rpcids (`c3wobe`/`L5adhe`/`ESY5D`/`PCck7e`/`aPya6c` with `aPya6c` firing first on submit and aborting the flow during Offline). (4) All 5 Warning Delivery Options verdict: A ❌ (not SSE) · **A' wrb.fr envelope replacement ⚠️ only marginal path** · B ❌ · C ❌ · D ❌ (CSP) · E ❌ (SPA session break). (5) Most likely failure mode of existing Strategy D = protobuf schema mismatch, NOT wrong approach. Wrote **services/gemini3_frontend.md** + updated **services/gemini_design.md** with Phase 5 debugging checklist (capture raw success body → byte-level diff APF output → fix field differences → retest). If byte-identical envelope still silent-fails → reclassify PENDING_INFRA. **gemini3 status: phase4_frontend_inspect → phase5_schema_debug_required.** Then **drafted + pushed #453 github_copilot frontend-inspect** (priority 5, NEEDS_ALTERNATIVE pathfinding modeled on #451/#452): captures copilot API endpoint, error UI via Offline + URL-block, CSP, 5-way delivery evaluation. Core question: does GitHub frontend read copilot_403 envelope body or does it only see status code and render its own error page? Login + subscription required → NEEDS_USER_SESSION fallback if not met. Rate Limit Gate: 1/2. |
| 17:22 | **Cycle 18 — DeepSeek #451 Phase 4+5 double delivery (outstanding result).** #451 result arrived 17:18 (23min turnaround). Verdict: `FRONTEND_PROFILED — Option A viable`. Full SSE wire format captured: named events (ready/update_session/title/close) + unnamed JSON-Patch ops (APPEND/SET/BATCH/SETLASTIDX) with **path inheritance rule** (`{v:...}` alone inherits `p/o` from previous patch — that's how DeepSeek streams tokens). Chat bubble renders off `state.response.fragments[-1].content` → **single patch can deliver full warning**. Error UI = static i18n `네트워크를 확인하고 다시 시도하세요.` → Option C blocked definitively. CSP null, custom PoW pass-through safe, Volcengine telemetry noise. 5-way verdict: A ✅ STRONG / B ❌ fetch-parser / C ❌ i18n / D ❌ network-layer / E ❌ SPA. Wrote **services/deepseek_frontend.md** (Phase 4) + **services/deepseek_design.md** (Phase 5) directly from the result — no sub-agent roundtrip needed, the capture was that clean. Envelope sizing: minimal ~820B raw → trimmed ~515B → final ~469B under 500B `h2_end_stream=2` ceiling using `⚠️ 보안 정책` Korean warning text. Phase 6 migration SQL embedded: UPDATE `ai_prompt_services` + `ai_prompt_response_templates`, pre-check SELECT, revision_cnt bump, single transaction. Schema drift monitoring plan + test criteria (5 items) + bo_mlog_info test log template all documented. **deepseek: phase4_frontend_inspect → phase5_designed**, joins v0 in phase6_db_blocked bucket. Rate Limit Gate: 2/2 → 1/2. |
| 17:12 | **Cycle 17 — v0 Phase 6 design upgrade** (dev-side parallel work while Rate Limit Gate 2/2 full). Code-read `ai_prompt_filter_db_config_loader.cpp:198-296` → `detect_service` uses priority-based multi-candidate matching (domain_priority + path_priority). **결정적 finding**: f+h를 **두 개의 row** (`v0` + `v0_api`)로 구현 가능. Row A=`/chat` → v0_html_block_page (우선순위 1005), Row B=`/chat/api/send` → v0_303_redirect (우선순위 1014). Row B가 `/chat/api/send`에서 Row A를 이기고, Row A는 `/chat/<id>` reload를 단독으로 캐치함. `is_prepare_api`는 `/prepare` suffix에 하드코딩(ai_prompt_filter.cpp:525-526)되어 repurpose 불가 확인. `v0_design.md` 업데이트 커밋 `85ebc23`: schema finding + 4-scenario 테스트 매트릭스 + 통합 SQL(INSERT ON DUPLICATE for v0_api + UPDATE for v0) + pre-flight conflict check query. v0 Phase 6 migration은 이제 DB access만 열리면 single-transaction 실행 가능. |
| 17:05 | **Cycle 16 — #452 gemini3 frontend-inspect pushed** (parallel to #451, Rate Limit Gate 2/2). HIGH priority, 10-15분 예상. 목표: Strategy D(END_STREAM-only wrb.fr) 설계가 실제로 구현 가능한지 근본 재검증 — '조용히 실패' 상태의 실제 DOM 증상(정말 UI가 없는가? 숨김 에러 hook이 있는가?) + CSP 전문 + 5 warning delivery options. Angular SPA 특성 고려. #451은 여전히 in flight (10분 경과, 아직 결과 미도착 — test PC가 request를 pickup하려면 polling cycle 필요). |
| 16:55 | **Cycle 15 PIVOT — DEV_DRIVES mode.** User correction: git pull에 아무것도 없는 이유는 test PC가 멈춘 게 아니라 내가(dev PC) request를 push하지 않아서. **즉시 #451 deepseek frontend-inspect 드래프트+push** (Phase 4 NEEDS_ALTERNATIVE pathfinding, v0 #447 패턴 모델링): chat.deepseek.com 로그인 → baseline SSE 캡처 → DevTools Offline로 에러 UI 유도 → DOM/CSP/5 Warning Delivery Options 평가 → 권장 옵션 1-2개. HIGH priority, 8-12분 예상. Rate Limit Gate 1/2. **운영 원칙 재정립**: dev PC가 작업을 드라이브한다. idle poll 금지. 매 cycle: pull → 결과 처리 → 다음 probe draft/dispatch → wake schedule. |
| 16:42 | **Cycle 13 idle poll.** No new results (last=#450); heartbeat.json still Apr 14 15:14 (test PC polling stopped). etap.log today [I] highlights: 09:23 APF_WARNING_TEST block events (keepalive + vts_rst_server, source unknown), 11:25 `[APF] WebSocket upgrade detected for AI service: m365_copilot` at ai_prompt_filter.cpp:586/:859. **`git log --all -S 'WebSocket upgrade detected for AI service'` returns ZERO hits** across ENTIRE repo history → a THIRD running-binary log string (after :722 Page load) absent from source tree. Source-drift evidence keeps accumulating → reinforces gamma PENDING_INFRA. No AI traffic on test PC 11:25→16:41. Cycle 14 at 16:46. |
| 16:36 | **Cycle 12 (25h gap after cycle 11 — session was suspended).** Wake fired with full state change on test server. (1) etap.log writer RECOVERED on its own — today 1086 entries, 56 healthy `[I]` APF blocks firing throughout the day (01:36, 03:48, 07:49, 08:21, 09:23, …). L2 SSH monitoring is **restored**. (2) SetCertificate failed histogram today: 85/hour baseline at even hours (00,02,04,…,12,14) = a cron-client retry loop, NOT gamma-specific. Yesterday's cycle 9 "38 errors at 14:34 = gamma cert failure" correlation was circumstantial — it was baseline noise overlapping with the gamma probe window. **Further strengthens cycle 11 reversal.** (3) Running binary emits `[APF] Page load request (Accept: text/html) for claude.ai/epitaxy (H2)` from `ai_prompt_filter.cpp:722` — confirmed the production binary has a different line numbering and a page-load detection path not present in our worktree. (4) claude.ai/epitaxy TLS handshake succeeded at 08:04 KST — another data point that TLS path works for non-failing hosts. (5) Test PC heartbeat stale for ~25h (last update 2026-04-14 15:00:04, cycle 23, last_processed_id 449). User appears to have paused test PC polling. No new results committed since #450. (6) Dev-side stance: dispatching new probes is futile without test PC polling. Remain in idle-monitoring state; each cycle poll + sample etap.log for new signals; resume active work when test PC heartbeat refreshes OR user opens v0 DB access window. |

## Service queue (autonomous rotation)

| # | Service | Status | Notes |
|---|---------|--------|-------|
| 1 | v0 | phase6_db_blocked | Awaiting DB access to 218.232.120.58 for f+h migration |
| 2 | gamma | **pending_infra** | 3-cycle investigation refuted APF/TLS/cert hypotheses; source-tree drift + L2 blind spot prevent dev-side diagnosis. Needs user/infra action. |
| 3 | **deepseek** | pending_check → phase1_har_capture | BLOCK_ONLY, h2_end_stream=2 class. Active focus as of 2026-04-14 15:20. |
| 4 | gemini3 | pending_check | BLOCK_ONLY, POST /StreamGenerate |
| 5 | github_copilot | pending_check | BLOCK_ONLY, copilot_403 |
| 6 | huggingface | pending_check | BLOCK_ONLY, streaming abort |
| 7 | baidu | pending_check | BLOCK_ONLY, ERNIE ignores SSE |

## Done services (6)

chatgpt · claude · genspark · blackbox · qwen3 · grok

## Classification snapshot

| 분류 | Count | 비율 |
|------|-------|------|
| DONE | 6 | 20.7% |
| BLOCK_ONLY | 10 | 34.5% |
| NEEDS_ALTERNATIVE | 9 | 31.0% |
| DISCONTINUED / REGION / DISABLED / SPECIAL | 12 | — |

Total registered: 37 · Total active: 29

## Next actions (autonomous queue)

1. Load `shared-skills/genai-apf-pipeline/references/phase1-har-capture.md` for Phase 1 procedure
2. Check `services/status.md` and any existing deepseek experience files (`services/deepseek_*.md`)
3. Draft Phase 1 HAR capture request for deepseek → push as #451 (logged-in chat.deepseek.com, SSN keyword, SSE stream)
4. Flag etap.log silence (`/var/log/etap.log` last write 14:59:28) as a side-task for ops check (restart etap, verify logger health) before trusting L2 signals on future probes
5. v0 Phase 6 still blocked on DB access — resume when user opens DB window
6. gamma → PENDING_INFRA, do not re-probe until (a) user reviews running-binary source, (b) etap logger restored, (c) infra confirms Cloudflare/visible_tls state

## Monitoring layers

| Layer | 수단 | 상태 |
|-------|------|------|
| L1 | git polling (results/) | **active** — 270s ScheduleWakeup loop (dev PC). Test PC L1 polling SUSPECTED-OFF (heartbeat stale 25h+). |
| L2 | SSH etap 로그 | **RESTORED** (cycle 12) — `/var/log/etap.log` writing normally today (2026-04-15). Baseline noise: ~85 `SetCertificate failed` every 2h (cron client, NOT gamma). Healthy APF blocks firing throughout day. |
| L3 | AnyDesk 시각 진단 | visual_needed=false |
